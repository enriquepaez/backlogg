meta {
  name: Post series log
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/series/breaking-bad/log
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {}
}

tests {
  test("status 201", function() {
    expect(res.status).to.equal(201);
  });

  test("defaults applied", function() {
    expect(res.body.item_type).to.equal("SERIES");
    expect(res.body.rewatch).to.equal(false);
  });
}
