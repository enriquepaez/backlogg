meta {
  name: Get similar books — not found (404)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/v1/books/slug-that-does-not-exist/similar
  body: none
  auth: none
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
