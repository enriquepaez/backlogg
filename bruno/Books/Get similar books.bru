meta {
  name: Get similar books
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/v1/books/harry-potter-and-the-philosophers-stone-1997/similar
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });

  test("results field is an array", function() {
    expect(res.body.results).to.be.an("array");
  });
}
