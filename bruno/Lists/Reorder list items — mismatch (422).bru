meta {
  name: Reorder list items — mismatch (422)
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/v1/lists/best-sci-fi/items/order
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "items": [
      { "item_type": "movie", "slug": "otra-cosa" }
    ]
  }
}

tests {
  test("status 422", function() {
    expect(res.status).to.equal(422);
  });
}
