meta {
  name: Create list — sin token (401)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/lists
  body: json
  auth: none
}

body:json {
  {
    "title": "No Auth"
  }
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
