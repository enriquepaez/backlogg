meta {
  name: Delete avatar
  type: http
  seq: 23
}

delete {
  url: {{baseUrl}}/v1/users/me/avatar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
