meta {
  name: Delete movie library
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/movies/oppenheimer-2023/library
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
