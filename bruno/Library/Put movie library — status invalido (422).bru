meta {
  name: Put movie library — status invalido (422)
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/movies/oppenheimer-2023/library
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "status": "invalido"
  }
}

tests {
  test("status 422", function() {
    expect(res.status).to.equal(422);
  });
}
