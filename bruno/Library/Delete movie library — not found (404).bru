meta {
  name: Delete movie library — not found (404)
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/movies/oppenheimer-2023/library
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
