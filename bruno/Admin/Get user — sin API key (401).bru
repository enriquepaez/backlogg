meta {
  name: Get user — sin API key (401)
  type: http
  seq: 29
}

get {
  url: {{baseUrl}}/v1/admin/users/alice
  body: none
  auth: none
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
