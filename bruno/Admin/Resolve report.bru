meta {
  name: Resolve report
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/reports/1/resolve
  body: none
  auth: none
}

headers {
  X-API-Key: {{adminApiKey}}
}

tests {
  test("status 200 or 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}

docs {
  Mark a report as resolved (idempotent). 404 if the report id does not exist.
  Requires the X-API-Key header.
}
