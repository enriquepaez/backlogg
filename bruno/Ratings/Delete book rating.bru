meta {
  name: Delete book rating
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/v1/books/harry-potter-and-the-philosophers-stone-1997/rating
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
