meta {
  name: Delete series rating
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/v1/series/the-bear-2022/rating
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
