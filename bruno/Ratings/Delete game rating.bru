meta {
  name: Delete game rating
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/v1/games/baldurs-gate-iii/rating
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
