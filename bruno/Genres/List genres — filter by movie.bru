meta {
  name: List genres — filter by movie
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/genres?type=movie
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.genres: isDefined
}
