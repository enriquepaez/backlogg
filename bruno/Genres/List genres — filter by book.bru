meta {
  name: List genres — filter by book
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/genres?type=book
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.genres: isDefined
}
