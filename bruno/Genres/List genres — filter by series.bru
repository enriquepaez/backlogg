meta {
  name: List genres — filter by series
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/genres?type=series
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.genres: isDefined
}
