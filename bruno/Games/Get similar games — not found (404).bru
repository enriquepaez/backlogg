meta {
  name: Get similar games — not found (404)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/v1/games/slug-that-does-not-exist/similar
  body: none
  auth: none
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
