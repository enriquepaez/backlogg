# Historial de sesiones

<!-- Append-only. Añadir una línea al cerrar cada feature. -->
<!-- Formato: YYYY-MM-DD | feat_<id> <name> | resumen de una línea -->
2026-08-16 | feat_46 books_similar_local | backlogg/books/repository.py (`get_author_person_ids`, `get_books_by_same_authors` vía Credit role="AUTHOR" de la feature 19, `get_books_by_genre_overlap` como fallback) + backlogg/books/service.py (`get_similar_books`: 404 si el slug no existe, tier autor > tier género, excluye el propio libro y duplicados, desempate por rating_external desc, tope de 10, sin llamadas externas ni external_ids nuevos) + backlogg/books/schemas.py (SimilarBookOut/SimilarBooksOut) + backlogg/books/routes.py (GET /v1/books/{slug}/similar, mismo contrato results[] que /movies/{slug}/similar pero sin campo reason) + docs/api.md (documenta el endpoint y la ausencia de reason) + bruno/Books/ (2 requests nuevos) + tests/books/test_similar_service.py (8 tests: prioridad autor>género, fallback a género, exclusión propia, desempate por rating, vacío sin autor/género, límite de 10, forma de respuesta, 404) + tests/books/test_router.py (200/404 a nivel endpoint). 708 tests en verde. Reviewer: APPROVED sin cambios requeridos (verificación propia de capas, init.sh, y de que no hay fan-out externo). Pendiente de checklist de QA manual + confirmación del usuario antes de commit/push/PR.
2026-08-16 | FE-27 (id 26) recommendations | apps/web/src/lib/recommendations.ts (+test) — `getRecommendations` vía `apiFetch` contra `GET /v1/recommendations` (auth, `?type=`/page/limit), `RECOMMENDATIONS_FALLBACK_REASON="Popular right now"` (coincide literal con `backlogg/recommendations/service.py`) y `recommendationItemType` (mapea `item_type` del backend a la ruta) + apps/web/src/components/recommendation-filters.tsx y recommendation-pagination.tsx (+tests) — selector de tipo y paginación prev/next vía heurística `hasNext = results.length === limit` (el backend no devuelve `total`) + apps/web/src/app/[locale]/recommendations/page.tsx + loading.tsx — página auth-gated (doble capa: `protected-routes.ts` en middleware + `getCurrentUser()` en el propio Server Component, mismo patrón que `/feed`), banner `role="status"` cuando todos los resultados de la página comparten el reason de fallback, `reason` de cada item mostrado sin traducir (contenido de dominio, no UI) + `catalog-card.tsx` extendido con prop opcional `footer` (retrocompatible, 3 usos previos sin romper) + `site-header.tsx` con entrada de nav solo con sesión + namespaces i18n `Recommendations`/`Metadata.recommendations`/`Nav.recommendations` en es/en. Pipeline verde: typecheck/lint/build + 573 tests frontend + `bash init.sh` (692 backend, intacto). Reviewer: APPROVED sin cambios requeridos, verificación propia del pipeline completo y del diff real. Pendiente de checklist de QA manual + confirmación del usuario antes de commit/push/PR.
2026-08-15 | reversión de producto: retirada completa de "listas curadas" (backend feature 32 `user_lists` + frontend FE-25 `lists_crud`/FE-26 `list_items`, rama `chore/remove-user-lists`) | Decisión explícita del usuario tras QA manual de FE-26: durante la prueba del selector de items ("El padrino" no encontraba *The Godfather* porque la búsqueda local devolvía 3 coincidencias débiles y eso bloqueaba el fallback a TMDB, feature 17 `search_external_fallback` — comportamiento intencional pero de baja relevancia), el usuario concluyó que no le ve sentido al producto de listas y pidió retirarlo por completo (no solo el hallazgo puntual). Confirmado que nada depende de la feature 32 backend ni de FE-25/26 frontend antes de tocar nada. FE-26 (implementado sobre `feat/list_items`, nunca comiteado) se descartó sin pérdida (solo working tree). Backend: `git revert -m 1 d6b059e` (PR #58) del dominio `backlogg/lists/` + `tests/lists/` + `bruno/Lists/` + wiring en `main.py`, resuelto a mano por conflictos con `api_versioning`/`content_moderation` posteriores; la migración `alembic/versions/0013_user_lists.py` se dejó intacta (ya aplicada en producción/Neon vía deploy de Render — reescribir una migración ya corrida rompe la cadena de Alembic) y en su lugar se añadió `alembic/versions/0019_drop_user_lists.py` (down_revision="0018", DROP de `list_items`/`user_lists` en orden FK-safe, downgrade que recrea el schema exacto de 0013 — ciclo upgrade→downgrade→upgrade verificado por el reviewer contra la DB de dev). Limpieza de residuales: `backlogg/users/repository.py` (docstring de cascada), `tests/users/test_account_deletion.py` (test de cascada creaba una `UserList`), `tests/test_openapi_schema.py` (esperaba el tag/paths/schema `lists`). Entrada `id: 32` eliminada de `feature_list.json`; secciones de lists quitadas de `docs/api.md`/`docs/schema.md`. Frontend: `git revert -m 1 17d51fc` (PR #104, FE-25) sin conflictos, más limpieza manual de lo que predataba ese PR (la sección de listas públicas de solo lectura en `/u/{username}` y `getUserLists`/`UserListSummary` en `lib/user-content.ts` venían de FE-21, no de FE-25/26 — el revert automático no las tocaba) y de las claves i18n `Profile.lists.*` en `messages/{en,es}.json`. `packages/api-client` regenerado desde el backend ya revertido (`pnpm --filter @backlogg/api-client gen:api`, 59 paths, sin ningún path/schema de lists). Entradas `id: 26` (FE-25) e `id: 27` (FE-26) eliminadas de `frontend_feature_list.json`; milestone M5 renombrado de "Listas y recomendaciones" a "Recomendaciones" (solo queda FE-27 `recommendations`, pendiente). Verificado que ninguna otra feature de ninguno de los dos backlogs quedó con una referencia colgante a los ids retirados. Pipeline verde: `bash init.sh` (692 backend) + typecheck/lint/559 tests frontend/build (sin ninguna ruta `/lists` ni `/api/lists/*` en el árbol generado). Reviewer backend: APPROVED en texto (sin `progress/review_*.md`, cambio fuera de backlog), con verificación independiente exhaustiva de la migración 0019 (ciclo completo, diff línea a línea contra 0013, ausencia de referencias residuales). Revert frontend hecho directamente por el leader (sin subagente frontend dedicado, mismo patrón que sesiones anteriores) y re-verificado por el propio leader. Ronda de cierre a petición del usuario ("reenumera las features" + "haz el QA tú", mismo commit/PR): (1) `feature_list.json` y `frontend_feature_list.json` renumerados sin huecos (backend 33-45→32-44, frontend 28-32→26-30) vía script que remapea también todos los `depends_on`, verificado sin referencias colgantes; (2) barrido final de residuales de "lists"/"listas" en todo el repo (backend, frontend, docs) más allá de los dos merges revertidos — encontrados y corregidos: descripción de cascada de `DELETE /v1/users/me` en `backlogg/users/routes.py` (expuesta en OpenAPI) y su docstring en `service.py`, comentario de `backlogg/notifications/models.py` (delegado a un implementer, cambio puramente textual, con regeneración de `packages/api-client` tras el fix), acceptance/description de FE-21 en `frontend_feature_list.json` (ya no muestra listas en el perfil), copy de confirmación de borrado de cuenta en `messages/{en,es}.json` (mencionaba "listas" entre los datos borrados), y menciones de scope en `docs/architecture.md`/`CLAUDE.md`. `docs/frontend-plan.md` (roadmap original pre-implementación) se dejó intacto deliberadamente — es un artefacto de planificación histórico, no el estado vivo. QA manual hecho por el leader (no delegado) contra backend+frontend reales corriendo en local (`backlogg-db` Docker + `uvicorn --reload` + `next dev` reiniciado para descartar mensajes i18n cacheados): `GET /v1/lists/*` y `/api/lists/*` → 404 reales; `/u/{username}` de un usuario de prueba real (`qa_a_*`) carga 200 sin ninguna mención de "lists" en el HTML servido y sin marcadores de error; confirmado que ninguna ruta bajo `/[locale]/[type]/[slug]` de dos segmentos arbitrarios (incluido "lists") se comporta de forma especial — es el catch-all genérico de la app, no un residuo. Pipeline final re-verificado en verde: `bash init.sh` (692 backend) + typecheck/lint/559 tests/build (frontend). Pendiente: confirmación explícita del usuario (en inglés) antes de commit/push/PR desde `chore/remove-user-lists`; ningún dato real de producción se pierde salvo que existan listas creadas por usuarios reales hoy en Neon (no verificado — el usuario no lo pidió).
2026-08-14 | FE-23 (id 24) activity_feed | apps/web/src/lib/feed-types.ts (`FeedTab`/`FEED_TABS`/`DEFAULT_FEED_TAB`/`isFeedTab`, sin `server-only` — split del vocabulario framework-agnostic respecto al fetch, mismo patrón que `catalog-types.ts`/`library-types.ts`) + apps/web/src/lib/feed.ts (`getFeed({tab,page,limit})` vía `apiFetch`, mismo esqueleto retry-on-401 que `getCurrentUser`) + apps/web/src/components/feed-tabs.tsx (Server Component, mismo shape que `StatusTabs`/`TypeTabs`, solo genera los 2 hrefs válidos — un tab inválido nunca llega al backend) + feed-pagination.tsx (mismo patrón que `FollowPagination`, preserva `tab` en el query param) + feed-entry-list.tsx (autor enlazado a `/u/{username}`, item enlazado a `/{type}/{slug}`, score/review_text/like_count/fecha; `feedItemType` duplica `toCatalogType` deliberadamente para no arrastrar `server-only` a sus tests, mismo criterio que `trendingItemType`) + app/[locale]/feed/page.tsx (defense-in-depth de sesión vía `getCurrentUser()` sobre la protección de `middleware.ts`, `parseTab` cae a `DEFAULT_FEED_TAB` ante cualquier valor no reconocido, estados vacíos distintos por tab, loading/error) + link "Feed" condicional en `site-header.tsx` (única forma de descubrir la ruta, no pedido explícitamente pero necesario) + namespaces i18n `Feed`/`Metadata.feed`/`Nav.feed` en es/en + 4 archivos de test nuevos. Pipeline verde: typecheck/lint/build + 502 tests (62 archivos). Reviewer: APPROVED sin cambios requeridos, verificación propia del pipeline y del contrato contra `docs/api.md`/`schema.d.ts`. Sin tocar `backlogg/` ni `tests/` de la raíz. Pendiente de checklist de QA manual + confirmación del usuario antes de commit/push/PR.
2026-08-14 | FE-23 (id 23) follows | apps/web/src/lib/follows.ts (`followUser`/`unfollowUser` dispatch — mismo shape que `likeReview`/`unlikeReview` de `ratings.ts` — + `getFollowers`/`getFollowing` públicos paginados, mismo `ok`-tagged shape que `getUserLibrary`) + apps/web/src/app/api/users/[username]/follow/route.ts (BFF: GET siempre-200 con `{authenticated, following}` — sin endpoint dedicado "¿le sigo?", workaround documentado de escanear la primera página, limit=100, de `getFollowing(viewer,...)`, mismo trade-off que el "no hay endpoint de mi rating" de `ratings.ts`; POST/DELETE proxy vía `apiFetch`) + apps/web/src/components/follow-widget.tsx (client, un solo componente para el link "X followers" + el botón follow/unfollow porque el contador necesita actualizarse optimistamente junto al botón; mismo patrón `pending`-guard + optimistic + rollback + toast que `ViewerStatusSlot`; oculto por completo en el propio perfil) + apps/web/src/components/follow-user-list.tsx (lista server-rendered, mismo patrón avatar/iniciales que `ReviewAuthor`) + apps/web/src/components/follow-pagination.tsx (paginación prev/next parametrizada por `href`, compartida por followers/following — a diferencia de `LibraryPagination` vs `ProfileReviewsPagination`, aquí ambas rutas tienen el mismo shape de query param) + páginas nuevas `u/[username]/followers` y `u/[username]/following` (mismo esqueleto que `library/page.tsx`) + `page.tsx` del perfil actualizado (followersCount → `FollowWidget`, followingCount → `Link`) + namespaces i18n `Profile.follow` y `Follows` en es/en. No existen subagentes `implementer`/`reviewer` para frontend (`docs/frontend-plan.md` §9, ítem abierto) — implementado y auto-verificado directamente por el leader, mismo patrón que FE-19/20/21. Pipeline verde: typecheck/lint/build + 483 tests (58 archivos, incl. `follows.test.ts`, BFF `route.test.ts`, `follow-widget.test.tsx`, `follow-pagination.test.tsx`, `follow-user-list.test.tsx` nuevos). Verificación adicional en navegador real (Playwright headless vía `@playwright/test`, backend local levantado contra `backlogg-db` + dos usuarios de prueba creados y borrados vía `DELETE /v1/users/me` al terminar, sin dejar fixtures — mismo cuidado que Issue #5): login real, follow optimista con contador actualizándose, follow persistido tras recargar, aparece en `/u/{followed}/followers` y en `/u/{follower}/following`, unfollow revierte botón y contador, sin botón follow/unfollow en el propio perfil. Screenshots confirman el render (avatar/iniciales, botón "Unfollow" con `aria-pressed`, contadores). Pendiente de checklist de QA manual + confirmación del usuario antes de commit/push/PR.
2026-08-14 | bugfix issue-10 ol_unfollowed_redirects (fuera de backlog, encontrado verificando Issue #9 con workflow_dispatch manual contra prod) | Tras mergear el fix de Issue #9, un `workflow_dispatch` manual (run 31777598168) confirmó `success`, pero `sync_books` reportó `people_errors: 4`. Logs reales de Render (aportados por el usuario) mostraron la causa exacta: `get_work_detail` pedía `GET /works/{id}.json` para 4 work_ids con sufijo `M` (edición, no obra — `OL8796283M`, `OL21145161M`, `OL19733909M`, `OL9340403M`) que Open Library redirige 301 a `/books/{id}.json`; el `httpx.AsyncClient` no seguía redirects (`follow_redirects` default `False`), así que `raise_for_status()` lanzaba excepción sobre la redirección no seguida. No era flakiness — el retry con tenacity de Issue #9 (mismo día) no lo habría arreglado, es determinista. Fix: `backlogg/books/adapters/open_library.py` — `get_work_detail`/`get_author` ganan `follow_redirects=True`; nueva `_normalize_edition_as_work` mapea el shape de una respuesta de edición (`authors` plano, `publish_date`, sin `description`) al shape de work que `_persist_book_authors`/`book_to_dict` esperan — verificado campo por campo contra la API real de OL para los 4 IDs de producción (2 sin autor en absoluto, comportamiento `[]` correcto; 2 con autor único en formato plano). `search_book` sin tocar (pega contra `/search.json`, no aplica esta clase de redirect). Tests nuevos en `test_open_library_adapter.py`, 3 de ellos confirmados por el reviewer como no-tautológicos vía `git stash` del archivo de producción. Issue #10 registrado en `issues_list.json`. `bash init.sh` en verde (708 tests). Reviewer: APPROVED (verificación propia con curl real contra los 4 IDs de producción, lectura de `_persist_book_authors`, confirmación de que 404 sigue detectándose tras seguir redirects, y de que Issues #7/#8/#9 no se tocaron). Pendiente de confirmación del usuario para commit/push/PR.
2026-08-14 | bugfix issue-9 book_sync_ol_5xx (fuera de backlog, encontrado investigando el nightly sync fallido de anoche) | El fix de Issue #8 (URLs /v1) quedó verificado: el run del 2026-08-14 04:00 UTC sincronizó movies/series/games con errors:0 por primera vez desde el 08-10. El job siguió fallando por un problema distinto: `sync_books` devolvió `synced:0/errors:1` en offset 3100 tras agotar 5 reintentos manuales (backoff lineal, ~30s) contra 500s intermitentes de Open Library, y el freshness check (ventana 2h) abortó todo el workflow. Confirmado en vivo que el mismo offset volvía a responder 200 minutos después — ventana de degradación de OL más larga que el presupuesto de reintento, no una regresión del fix de ayer. Fix: `backlogg/books/adapters/open_library.py` (`_fetch_popular_page` migrado a tenacity, mismo patrón que TMDB en Issue #7 — retry solo en 429/5xx/timeout/transport, nunca 4xx — 8 intentos con backoff exponencial 2/4/8/16/30/30/30s, ~120s de presupuesto vs. los ~30s previos; contrato de "sin páginas parciales" preservado) + `.github/workflows/nightly-sync.yml` (cada paso de sync reintenta una vez su curl si el JSON de respuesta trae errors>0, antes de pasar al freshness check; la ventana de 2h del freshness check no se tocó) + tests actualizados en `tests/books/test_open_library_adapter.py` (reviewer confirmó con `git stash` que 3 tests fallan de verdad contra el código pre-fix, no son tautológicos) + Issue #9 registrado en `issues_list.json` con datos reales del run 31768514112. `bash init.sh` en verde (702 tests). Reviewer: APPROVED (verificación propia del diff, del YAML parseado, y de que Issues #7/#8 no se tocaron). Pendiente de confirmación del usuario para commit/push/PR — no toca `.github/workflows` en producción hasta el próximo cron tras el merge.
2026-08-13 | FE-21 user_profile | apps/web/src/app/[locale]/u/[username]/page.tsx (nueva página `/u/{username}`, cierra la forward-reference dejada por FE-19/FE-20) + apps/web/src/lib/user-content.ts (`getUserReviews`/`getUserLists`, mismo estilo público sin auth que `getUserProfile`/`getUserLibrary` de `library.ts`, separado en archivo propio porque `library.ts` se autolimita a library/backlog) + apps/web/src/components/profile-reviews-pagination.tsx (paginación de reviews del perfil, mismo shape que `LibraryPagination` pero con solo `page` como query param) + namespace i18n `Profile` en messages/{en,es}.json + doc-comments actualizados en `u/[username]/library/page.tsx` e `item-reviews.tsx` (ya no describen `/u/{username}` como ruta no shipeada). Cabecera con followers/following (texto plano, sin enlace — FE-22 pendiente), resumen de biblioteca con link a `/u/{username}/library`, reviews paginadas, listas públicas de solo lectura (sin enlace — `/lists/{slug}` es FE-26, no implementada aún), distingue perfil propio (link a `/settings`) vía `getCurrentUser()`. `getUserLists` sin token de auth (mismo patrón de Server Component público que el resto del repo) — efecto documentado: el owner no ve sus listas privadas en su propio perfil todavía, aceptable para el scope de FE-21. Pipeline verde: typecheck/lint/build + 443 tests (53 archivos, incl. `user-content.test.ts` y `profile-reviews-pagination.test.tsx` nuevos). Reviewer: APPROVED sin cambios requeridos (verificación propia del pipeline y de los diffs reales). Ajuste post-QA (mismo commit/PR, tras probar en navegador): el usuario reportó que faltaba una entrada de menú para llegar al perfil propio y que la sección de biblioteca del perfil daba poca información a primera vista con solo counts en texto plano. Fix: `user-nav.tsx` gana una entrada "My profile"/"Mi perfil" (icono `CircleUserRound`, mismo patrón `DropdownMenuItem asChild`+`Link` que "Settings") enlazando a `/u/{username}`, justo antes de Settings; `LibrarySummary` en `page.tsx` gana un preview real de hasta `LIBRARY_PREVIEW_SIZE=6` items (`getUserLibrary`, mismo `CatalogCard` grid que `/u/{username}/library`) debajo de los counts, omitido silenciosamente si está vacío (sin empty-state adicional, los counts en 0 ya lo comunican). Namespace `Nav.profile` nuevo en es/en. typecheck/lint/build/444 tests en verde, re-verificado por el leader. QA manual en navegador pendiente de confirmación del usuario antes de mergear.
2026-08-13 | FE-19 reviews_display_likes | apps/web/src/app/api/[type]/[slug]/ratings/route.ts (BFF GET público, proxea GET /v1/{type}/{slug}/ratings paginado, añade flag `authenticated` vía getCurrentUser()) + apps/web/src/app/api/reviews/[id]/like/route.ts (BFF POST/DELETE, apiFetch con auto-refresh, mismo esqueleto que report/route.ts de FE-18) + apps/web/src/components/item-reviews.tsx (`ItemReviews`: lista paginada con autor enlazado a /u/{username} — ruta reservada por FE-21 aún no implementada, forward-reference documentada —, estrellas, texto, contador de likes, like/unlike optimista) montado en [locale]/[type]/[slug]/page.tsx bajo el RatingWidget de FE-18 + lib/ratings.ts (likeReview/unlikeReview) + namespace ItemDetail.reviews en messages/{en,es}.json. 1ª ronda de review: CHANGES_REQUESTED — el botón de like/unlike no se deshabilitaba durante la petición en vuelo (riesgo de doble-submit y de que un revert desordenado pisara un estado más reciente); fix: campo `pending` por review en `LikeState`, no-op si ya hay una petición en vuelo, botón deshabilitado mientras dura, resuelto en el mismo try/catch/finally. Test nuevo de doble-click cubre el caso. Pipeline verde: typecheck/lint/build + 375 tests frontend + bash init.sh (702 backend, sin tocar backlogg/ ni tests/). Reviewer: APPROVED en 2ª ronda (verificación propia del diff, del test nuevo y re-ejecución completa del pipeline).
2026-08-12 | FE-14 session_ux | Gran parte de la infraestructura de sesión (refresco proactivo del access token en `proxy.ts`/`lib/auth/proxy-refresh.ts`, protección de rutas privadas, header session-aware) ya existía como efecto colateral de FE-4/FE-5; esta feature cerró los huecos reales. `apps/web/src/components/user-nav.tsx` reescrito como `DropdownMenu` de Radix/shadcn (antes era avatar+nombre+botón inline; el criterio de aceptación pide literalmente "menú de cuenta" y FE-17 `account_settings` necesitará colgar más entradas ahí) + `apps/web/src/components/ui/dropdown-menu.tsx` (primitives nuevas) + `lib/auth/use-logout.ts` (lógica de logout extraída, compartida entre el item del menú y `logout-button.tsx` standalone) + tests unitarios nuevos (`user-nav.test.tsx`, `logout-button.test.tsx`, `site-header.test.tsx`, cubren anónimo/autenticado, apertura/cierre del menú con Escape/click-fuera vía `pointerDown` real de Radix, y la llamada real a `/api/auth/logout`) + `e2e/session-ux.spec.ts` + `e2e/mock-backend.mjs` (mock HTTP mínimo sin dependencias para `/v1/auth/refresh`/`logout`/`users/me`, honesto sobre no simular reuse-detection real) + `playwright.config.ts` (`webServer` pasa a array: mock-backend + `next dev` apuntando a él, en vez de un puerto inalcanzable — necesario porque Next solo permite una instancia de `next dev` por proyecto y verificar el refresh real requiere un backend que responda de verdad; confirmado que `item-detail.spec.ts`/`browse.spec.ts` preexistentes siguen en verde) + `next.config.ts` (`allowedDevOrigins: ["127.0.0.1"]`, fix incidental dev-only: Playwright navega contra `127.0.0.1` y `next dev` solo confiaba en `localhost`, bloqueando la hidratación del cliente — invisible hasta este e2e por ser el primero con interacción real). QA manual contra backend real (curl + Playwright/Chromium): borrado de `bl_access` manteniendo `bl_refresh` → siguiente request rota el par de verdad (confirmado intentando reusar el refresh viejo directo contra el backend → 401), sin redirect a login; logout limpia ambas cookies y una ruta protegida posterior redirige a `/{locale}/login`. Pipeline verde: typecheck/lint/build, 174 tests unitarios (27 archivos), 8 e2e (4 preexistentes + 4 nuevos), `bash init.sh` (690 tests backend, sin tocar `backlogg/`/`tests/`) — todo re-ejecutado de forma independiente por el reviewer (que además mató un `next dev` manual que el implementer había dejado vivo, para evitar que Playwright lo reutilizara en vez del mock). Reviewer: APPROVED. Único hallazgo no bloqueante: comentarios desactualizados en `item-detail.spec.ts`/`browse.spec.ts` ("deliberately unreachable", ya no literalmente cierto con el mock nuevo) — cosmético, no afecta comportamiento.
2026-08-11 | FE-13 register_login | apps/web/src/lib/auth/schemas.ts (registerSchema/loginSchema zod, espejo exacto de UserCreate/UserLogin del backend: username [a-zA-Z0-9_-]{3,50}, password min 8; applyZodFieldErrors mapea fallos de safeParse a react-hook-form sin depender de @hookform/resolvers) + lib/use-countdown.ts (hook de cuenta atrás extraído para el 429, en vez de forzar la reutilización de RateLimitNotice de FE-11 cuyo "retry" hace router.refresh() de Server Component, incompatible con un form cliente) + app/api/auth/register/route.ts (proxea POST /v1/auth/register vía getApiClient(); 201/409/422/429 con Retry-After, mismo patrón que login/route.ts existente de FE-4) + components/register-form.tsx y login-form.tsx (react-hook-form, validación cliente sin red antes de tocar el BFF) + app/[locale]/{register,login}/page.tsx (Server Components con generateMetadata, Card de shadcn, enlace cruzado) + namespace i18n Auth completo en es/en. El registro no devuelve tokens (solo UserMeOut) — register-form.tsx encadena un login automático tras el 201 para no pedirle credenciales dos veces al usuario; si ese login encadenado falla (rate limit entre ambas llamadas, error de red), se dispara toast.success(Auth.login.accountCreated) antes de redirigir a /login (fix aplicado tras la 1ª ronda de review, que señaló que el usuario aterrizaba en /login sin saber que su cuenta sí se había creado). react-hook-form y zod añadidos como dependencias nuevas de apps/web. Pipeline verde: typecheck/lint/build/test (24 archivos, 159 tests, 27 nuevos) + bash init.sh (690 tests backend, sin tocar backlogg/ ni tests/). Reviewer: APPROVED en 2ª ronda. Ajuste UX post-QA (mismo commit/PR, sin reabrir la feature): a petición del usuario tras probar el formulario en navegador, los errores de formato zod (username/email/password) pasan de validarse solo al submit a mostrarse en vivo (`mode: "onBlur"` + `reValidateMode: "onChange"` en ambos `useForm`, nuevo helper `fieldFormatRule` en schemas.ts que conecta zod al `validate` nativo de `register()` sin añadir `@hookform/resolvers`); alcance acotado explícitamente a formato — el duplicado de username/email (409) sigue detectándose solo al enviar, sin chequeos de red mientras se escribe. 161 tests en verde. Reviewer: APPROVED (2ª pasada, sin archivo `review_14.md` nuevo por ser ajuste incremental sobre feature ya cerrada). Segundo ajuste UX post-QA (mismo commit/PR): más espacio entre campos (`gap-4` → `gap-6` en ambos `<form>`) y el `<p>` de error por campo pasa de montarse/desmontarse condicionalmente a permanecer siempre presente con `min-h-[1.25rem]` (texto vacío cuando no hay error, `role="alert"` solo cuando sí lo hay) para eliminar el layout shift al aparecer/corregir un error de validación. Puro cambio visual, sin tocar lógica de submit/red; typecheck/lint/build/161 tests en verde. Verificado directamente por el leader (sin ronda de reviewer, por ser un cambio de estilo sin superficie de comportamiento). Tercer ajuste (revert parcial, mismo commit/PR): el usuario no quiso el resultado visual del `gap-6` + `min-h` (demasiado hueco muerto entre campos sin errores) — se revirtió a `gap-4` y a `<p>` de error siempre montado con clase `invisible`+placeholder cuando no hay error (reserva la altura real de una línea de `text-sm`, sin caja vacía visible) y sin `invisible`+`role="alert"`+texto real cuando sí lo hay. 161 tests en verde. Cuarto ajuste (mismo commit/PR): el `invisible` seguía reservando altura permanente incluso sin error, por lo que el ritmo vertical entre campos no coincidía con el original de FE-13 — contradicción irresoluble reservando espacio dentro del flujo normal. Solución final: el `<p>` de error vuelve al montaje condicional puro (ausente del DOM sin error), pero con `absolute top-full left-0 mt-1` sobre un grupo `relative`, sacándolo del flujo del documento — así su aparición/desaparición nunca afecta el alto del contenedor, y en reposo el espaciado es idéntico al original (sin gap-6 ni línea reservada). 161 tests en verde. Quinto ajuste (mismo commit/PR, dirección explícita del usuario — no un revert por disgusto): pidió lo contrario a la ronda 4: que el hueco del error exista de antemano dentro del flujo, no fuera de él. Se quitó `absolute`/`relative` y el `<p>` vuelve a montarse siempre en el flujo normal (`invisible` + placeholder sin `role` cuando no hay error, texto real + `role="alert"` cuando sí lo hay), con `gap-4` sin tocar. 163 tests en verde. Sexto ajuste (fix, mismo commit/PR): el usuario reportó que el hueco no existía hasta que salía el error — causa raíz encontrada por el leader directamente (sin ronda de implementer, cambio de un carácter en 6 sitios): el placeholder era un espacio normal `" "` (U+0020), que los navegadores colapsan por completo en un bloque sin más contenido, dejando el `<p>` con altura 0 hasta que hay texto real; sustituido por un espacio de no separación `" "` (U+00A0), que sí fuerza la caja de línea y reserva la altura de forma constante. typecheck/lint/build/163 tests en verde. Séptimo ajuste, definitivo (mismo commit/PR): el usuario reportó que aun así se había añadido espacio respecto al original Y que el alto seguía cambiando — verificado empíricamente por el leader con un script Playwright contra el dev server real (no solo tests unitarios): el hueco reservado en flujo (rondas 5-6) sí añadía ~26px permanentes por campo frente al original, Y el bloque de error a nivel de formulario (409 conflict/429/401/unknown, justo antes del botón) nunca se había tocado en ninguna ronda anterior y seguía desplazando el botón 36px al aparecer. Solución final: TODOS los mensajes de error (por campo y a nivel de formulario) se sacan del flujo con `position: absolute` sobre un contenedor `relative` — por campo, `absolute top-full left-0 mt-1` con montaje condicional puro (sin reservar nada); el bloque final envuelve el `Button` (ahora `w-full` porque deja de heredar el stretch del `flex` padre) en un `div relative` con los mensajes en `absolute bottom-full left-0 mb-2`, flotando justo encima del botón sin ocupar espacio propio. Verificado con Playwright headless contra localhost:3000 real: gap entre campos vuelve a 36px (frente a 62px de las rondas 5-6) y la posición Y del botón de submit permanece IDÉNTICA en reposo, tras error de formato, tras limpiar el error, y tras un 409 real de username duplicado. Tests actualizados. typecheck/lint/163 tests en verde. Octavo ajuste, definitivo (mismo commit/PR): el usuario aclaró que específicamente ENCIMA DEL BOTÓN de envío quería lo contrario a la ronda 7 — un hueco reservado en el flujo desde el principio (no `absolute`), para que el mensaje de error de nivel de formulario (409/429/401/unknown) aparezca ahí sin cambiar el alto ni montarse sobre el botón (el `absolute bottom-full` de la ronda 7 podía solaparse). Se unificaron los 4 mensajes posibles en una única variable `submitMessage`/`submitMessageTone` (solo uno puede estar activo a la vez) renderizada en un único `<p>` siempre montado (con ` ` como placeholder, la misma lección de la ronda 6) justo encima del `Button`, dentro de un `div flex flex-col gap-1.5` normal (sin `relative`/`absolute`/`w-full`, ya no hacen falta). Los errores por campo (rondas 4-7, `absolute top-full`) NO se tocaron — el usuario solo pidió este cambio para la zona del botón. Verificado con Playwright headless contra localhost:3000 real con backend vivo: la posición Y del botón es IDÉNTICA (511px) en reposo, con el formulario relleno sin error, y tras un 409 real de username duplicado; el mensaje de conflicto se renderiza 6px por encima del botón sin solaparlo. typecheck/lint/163 tests en verde. Noveno ajuste, definitivo (mismo commit/PR): el usuario pidió eliminar por completo los mensajes de error bajo cada input — todo error (formato por campo Y nivel de formulario) debe aparecer ÚNICAMENTE en el hueco reservado encima del botón. Se plegó `errors.username/email/password/displayName` (register) y `errors.username/password` (login) en la misma variable `submitMessage`, con prioridad: primer error de campo (en orden) > error de submit (409/429/401/unknown) — solo uno visible a la vez. Se eliminaron los `<p>` por campo y el `relative`/`absolute top-full` de sus contenedores (ya no hace falta, no hay overlay debajo de ningún input); `aria-invalid` en cada `Input` se mantiene para accesibilidad. Tests actualizados (los dos casos que esperaban 2-3 errores de campo simultáneos ahora esperan solo el primero, con `queryByText` confirmando que los demás NO aparecen). Verificado con Playwright headless contra localhost:3000 real: 0 elementos `<p>` bajo el input de username en reposo y tras un error de formato en vivo; el único `role="alert"` visible en toda la página es el del hueco sobre el botón; posición Y del botón constante (511px) en ambos estados. typecheck/lint/163 tests en verde. Décimo ajuste, definitivo (mismo commit/PR): el usuario pidió revertir la validación en vivo de la ronda 2 por completo — ningún error debe aparecer mientras se escribe o al salir de un campo (blur), solo al pulsar el botón de envío. Se quitó `mode: "onBlur"`/`reValidateMode: "onChange"` de ambos `useForm` (vuelven al default `onSubmit` de react-hook-form) y el `validate: fieldFormatRule(...)` de cada `register()` — la validación de formato la sigue cubriendo por completo `registerSchema.safeParse`/`loginSchema.safeParse` dentro de `onSubmit` (vía `applyZodFieldErrors`), que nunca se tocó en ninguna ronda. `fieldFormatRule` quedó sin uso y se eliminó de `schemas.ts` (dead code). Tests de "error en blur" reescritos a "sin error mientras se escribe ni en blur". Verificado con Playwright headless: 0 alerts en reposo, mientras se escribe un valor inválido, y tras el blur; exactamente 1 alert (con el texto correcto) tras pulsar el botón submit. typecheck/lint/163 tests en verde.
2026-08-10 | bugfix book_credits_empty_crash (fuera de backlog, encontrado en QA post-fix de book_credits) | El fix anterior de book_credits (mismo día) expuso un crash determinista: cualquier libro con `credits: []` (array vacío) rompía la página de detalle con `TypeError: Cannot read properties of undefined (reading 'length')` en `item-credits.tsx:35` (`credits.length`), tanto en SSR como en cliente. El leader aisló el bug con un A/B controlado (backend confirmado correcto por curl directo — `"credits":[]` presente en el wire, headers idénticos al caso que sí funciona; caché de disco `.next/dev/cache/fetch-cache` descartada, vaciada y el crash persistía; reinicio del proceso `next dev` tampoco lo resolvía) antes de delegar. Fix: `apps/web/src/app/[locale]/[type]/[slug]/page.tsx` (`getCredits` → `item.credits ?? []`; mismo patrón aplicado a `genres[]` y `game.platforms[]`, mismo riesgo latente encontrado en auditoría de campos hermanos) + `apps/web/src/components/item-credits.tsx` (segunda capa de defensa independiente, normaliza `credits ?? []` antes de `.length`/`.map`) + `item-credits.test.tsx` (nuevo test con `credits: undefined` contra el componente real). Causa raíz no cerrada del todo: instrumentación temporal (revertida sin residuo) mostró que la clave `"credits"` ya llegaba ausente del objeto parseado por `openapi-fetch`, antes de cualquier código propio — mecanismo exacto dentro de Next 16.3/Turbopack sin identificar, no bloqueante para el fix defensivo. Verificado de forma independiente tanto por el leader como por el reviewer en instancias temporales aisladas (backend + `next dev` en puertos alternos, sin tocar `.env.local` real ni los servidores del usuario): 4/6 y 6/6 renders limpios respectivamente contra el libro con credits vacío, sin regresión en el caso con credits no vacío. Pipeline backend (690 tests) y frontend (typecheck/lint/90 tests/build) en verde. Reviewer: APPROVED. Bundled en la misma rama/commit de ship que FE-10 y el fix de book_credits (feat/fe10-item-detail).
2026-08-10 | bugfix book_credits (fuera de backlog, encontrado en QA de FE-10) | backlogg/books/schemas.py (BookOut gana credits: list[CreditOut]) + backlogg/books/service.py (get_book/_book_to_out ahora llaman get_credits_for_item(db, "BOOK", book.id), mismo patrón que movies) + tests/books/test_router.py (2 tests nuevos: credits vacío y con autor) + docs/api.md (Books documenta credits[], nota general ampliada). Causa raíz: los autores ya se persistían como Credit role=AUTHOR vía _persist_book_authors desde FE-anterior de books, pero la API nunca los exponía — el dato existía en DB, faltaba en el contrato. Frontend (apps/web): page.tsx ya no excluye `book` al montar ItemCredits; heading renombrado de "Cast & crew"/"Reparto y equipo" a "Credits"/"Créditos" (neutro, sirve para reparto y para autor único) en messages/{en,es}.json; api-client regenerado (openapi.json/schema.d.ts); msw fixture de book con credit AUTHOR; item-credits.test.tsx cubre el caso de autor sin character_name. 2/4 e2e fallaban en el entorno compartido del implementer por un `next dev`/`uvicorn` reales ya corriendo (reuseExistingServer de Playwright, confirmado leyendo playwright.config.ts); el reviewer lo reprodujo de forma independiente en un clon aislado (4/4 en verde) y en el entorno compartido (2/4, mismo patrón) — confirmado como artefacto de entorno, no regresión. Backend 690 tests en verde, frontend typecheck/lint/89 tests/build en verde. Reviewer: APPROVED. Bundled en la misma rama y mismo commit de ship que FE-10 (feat/fe10-item-detail).
2026-08-10 | FE-10 item_detail | apps/web/src/app/[locale]/[type]/[slug]/page.tsx (Server Component, `generateMetadata` OG, fetch de detalle + similar en paralelo) + loading.tsx + item-hero.tsx/item-credits.tsx/item-similar.tsx (presentacionales, reciben strings ya traducidos) + viewer-status-slot.tsx (hueco no-op para auth-opcional, punto de enganche futuro FE-14/FE-20) + lib/catalog.ts (`getItemDetail` con resultado tri-estado ok/not-found/error — solo 404 real dispara `notFound()`, cualquier otro fallo se trata como transitorio y nunca se cachea como ausencia permanente; `getSimilarItems` corta a [] sin red para book/game) + catalog-card.tsx (prop `href` opcional, retrocompatible) + page.tsx home y browse/[type]/page.tsx (ahora enlazan tarjetas al detalle, cerrando el hueco que FE-8/FE-9 dejaron pendiente) + messages/{en,es}.json (namespace ItemDetail + Metadata.item) + msw handlers/fixtures + e2e/item-detail.spec.ts (backend inalcanzable → 200 con estado de error inline, no 404 duro). Pipeline verde (typecheck/lint/88 tests/build/4 e2e) re-ejecutado de forma independiente por el reviewer, más QA manual del implementer contra backend real (movie/series/book, credits/similar omitidos correctamente para book). Reviewer: APPROVED sin cambios requeridos. Backend intacto.
2026-08-09 | FE-8 home_landing | apps/web/src/lib/catalog.ts (getTrending/getFeatured/getAllFeatured, públicos sin cookies, reusan getApiClient() de lib/auth/session) + catalog-card.tsx/catalog-section.tsx (presentacionales, no enlazan a detalle de item — fuera de scope) + [locale]/page.tsx reescrito (Server Component async, generateMetadata OG) + next.config.ts (images.remotePatterns para image.tmdb.org/covers.openlibrary.org/images.igdb.com) + messages/{en,es}.json (namespace Home ampliado, retiradas exploreShowcase/documentation ya sin uso) + msw handlers/fixtures para /v1/trending y /v1/{series,books,games}. Revalidación real vía `next: { revalidate }` a nivel de fetch (trending 1h, featured 6h) porque `SiteHeader` fuerza SSR dinámico vía cookies() — confirmado en `next build` (`/[locale]` sigue `ƒ` Dynamic) y contra Next 16 docs/tipos reales. Enlaces a /browse/{type} y /search (404 esperado, FE-9/FE-11 pendientes, mismo patrón que /login). Degradación por sección ante fallo de API (try/catch → array vacío, nunca throw). 34 tests nuevos/existentes en verde + e2e smoke sin regresión. Reviewer: APPROVED (verificación propia de pipeline, hosts de imagen contra adapters reales, HTML en vivo contra backend local, paridad de claves en/es).
2026-08-09 | FE-5 app_shell | Layout raíz del frontend (apps/web, Next 16.3): `SiteHeader`/`SiteFooter` en `app/[locale]/layout.tsx` (nav refleja sesión vía `getCurrentUser()` server-side — `UserNav`/logout o link "Log in"; reusa `LanguageSwitcher`/`ModeToggle`), `loading.tsx`/`error.tsx` (prop `retry`, estable desde Next 16.3)/`not-found.tsx` localizados + catch-all `[locale]/[...rest]/page.tsx` para que sub-rutas inválidas bajo un locale válido lo alcancen, `global-error.tsx` como último boundary, `QueryProvider` (TanStack Query, `QueryClient` vía `useState` perezoso — NO singleton de módulo) y toasts con `sonner` (uso real en fallo de logout). Namespaces i18n nuevos (`Nav`/`Footer`/`Loading`/`NotFound`/`ErrorPage`) consistentes en/es. FIX obligatorio arrastrado de la review de FE-4: el bug de cookies-no-persistidas-en-render-RSC (refresh disparado durante el render revocaba en cascada TODAS las sesiones) se resolvió moviendo el refresh proactivo a `proxy.ts` (nuevo `lib/auth/proxy-refresh.ts`) — corre antes del render, escribe el par rotado en `request.cookies` (visible en la misma request) y lo refleja en `response.cookies` (persiste en el navegador); en fallo limpia ambas. Verificado E2E por implementer y reviewer por separado contra backend real: 3 ciclos de rotación limpia sin cascada (confirmado en `refresh_tokens` de Postgres), reuse-detection genuina sigue intacta, logout y redirect de rutas protegidas sin regresión. Residual documentado (no bloqueante): ventana muy estrecha de fail-open si el backend cae justo durante el refresh proactivo del proxy. Trade-off documentado: `/[locale]` y `/[locale]/showcase` pasan de estático a dinámico por leer cookies en el layout compartido (a considerar en M1/SSR-ISR). Pipeline verde (`pnpm --filter web typecheck && lint && build`, exit 0) re-ejecutado de forma independiente por el reviewer. Reviewer: APPROVED (sin cambios requeridos). Sin tocar backlogg/, tests/ ni .env real.
2026-08-08 | feat_40 response_caching | backlogg/core/cache.py (interfaz Cache ABC + InMemoryTTLCache con TTL por entrada/reloj inyectable/expiración perezosa + singleton get_cache()) + backlogg/core/http_cache.py (CacheHeadersMiddleware: clasifica por route template y aplica Cache-Control + ETag/304 en detalle; detalle anónimo public+max-age+ETag+Vary:Authorization, detalle autenticado private,no-cache, feed//users/me private,no-store) + config.py (CACHE_CONTROL_DETAIL_MAX_AGE=300, CACHE_CONTROL_LISTING_MAX_AGE=60, CACHE_TTL_TRENDING=900, CACHE_TTL_GENRES=300) + main.py (middleware más interno) + genres/service.py y trending/service.py (consultan get_cache() antes del repo) + conftest.py (fixture autouse _reset_response_cache) + .env.example + tests/core/test_cache.py + tests/test_response_caching.py (+18 tests). 632 tests en verde. Reviewer: APPROVED.
2026-08-08 | feat_38 observability | backlogg/core/observability.py (JSONFormatter stdlib, redacción de secretos por campos extra + texto libre/tracebacks incl. dict-repr/JSON/Bearer, configure_logging idempotente, ContextVar + RequestIDMiddleware con X-Request-ID entrante/generado + log request.completed método/path/status/duración, init_sentry perezoso gated por SENTRY_DSN) + config.py (LOG_LEVEL, SENTRY_DSN) + main.py (wiring + @app.exception_handler(Exception) 500 correlacionado) + pyproject.toml/uv.lock (sentry-sdk[fastapi]) + .env.example + docs/operations.md + tests/test_observability.py. 599 tests en verde. Reviewer: APPROVED (2ª ronda tras reforzar redacción AC3).
2026-06-21 | feat_22 cors_and_security | CORSMiddleware (CORS_ORIGINS env var, default localhost:3000+5173) + SecurityHeadersMiddleware (X-Content-Type-Options, X-Frame-Options, Referrer-Policy) en main.py + 8 tests. 185 tests en verde.
2026-06-21 | feat_21 credits_in_detail | shared/schemas.py (CreditOut) + shared/credits.py (get_credits_for_item JOIN credits+people ORDER BY billing_order ASC NULLS LAST) + MovieOut/SeriesOut/GameOut extendidos con credits[] + servicios actualizados para construir Pydantic explícito con credits inyectados + 6 tests nuevos (2 por dominio: empty y ordered). 164 tests en verde.
2026-06-21 | feat_19 book_authors_people | get_author() en OL adapter + _persist_book_authors() en service + sync_books() actualizado + _resolve_credits() extendido a BOOK + 8 tests nuevos. 158 tests en verde.
2026-05-24 | feat_1 shared_models | pyproject.toml + core (config, database) + shared/models.py (Person, Credit) + shared/external_ids.py (ExternalId, helpers upsert/get/set) + migración Alembic 0001 (external_ids, people, credits con triggers) + 5 tests en verde.
2026-05-24 | feat_2 movies | movies/models.py (Movie, MovieGenre, join) + movies/schemas.py (MovieOut, GenreOut) + movies/repository.py (get_by_slug, upsert con get-or-create genres) + movies/adapters/tmdb.py (TMDBClient: search + detail + slugify) + movies/service.py (on-demand fallback) + movies/routes.py (GET /movies/{slug}) + main.py (FastAPI app + /health) + migración Alembic 0002 + 14 tests en verde (4 repo + 3 servicio + 2 rutas + 5 heredados).
2026-05-24 | feat_3 series | series/models.py (Series, SeriesGenre, join) + series/schemas.py (SeriesOut, SeriesGenreOut) + series/repository.py (get_by_slug, upsert con get-or-create genres) + series/adapters/tmdb.py (TMDBSeriesClient: search_series /search/tv + get_series_detail /tv/{id} + series_to_dict) + series/service.py (on-demand fallback) + series/routes.py (GET /series/{slug}) + main.py actualizado + migración Alembic 0003 (series_genres, series, series_genres_join, trigger) + 8 tests nuevos, 22 en total. Reviewer: APPROVED, todos los checkpoints C1-C17, C20-C22 superados.
2026-05-24 | feat_4 books | books/models.py (Book, BookGenre, join) + books/schemas.py (BookOut, BookGenreOut) + books/repository.py (get_by_slug, upsert con get-or-create genres) + books/adapters/open_library.py (OpenLibraryClient: search_book /search.json + get_work_detail /works/{olid}.json + book_to_dict + _parse_ol_date) + books/service.py (on-demand fallback) + books/routes.py (GET /books/{slug}) + main.py actualizado + migración Alembic 0004 (book_genres, books, book_genres_join, trigger set_updated_at_books) + 9 tests nuevos (4 repo + 3 servicio + 2 router), 31 en total. Reviewer: APPROVED, todos los checkpoints C1-C17, C20-C22 superados. Open Library no provee rating (None). Géneros derivados de subjects (primeros 10). Slug = slugify(title)-year.
2026-05-24 | feat_6 people | backlogg/people/ (models.py, schemas.py, repository.py, service.py, routes.py) + movies/service.py (_persist_movie_people: ACTOR top10 + DIRECTOR) + series/service.py (_persist_series_people + _persist_series_creators: ACTOR top10 + CREATOR) + movies/adapters/tmdb.py (get_movie_credits) + series/adapters/tmdb.py (get_series_credits) + upsert idempotente en people/external_ids/credits + GET /people/{slug} (200 con créditos resueltos, 404 si no existe) + main.py actualizado + 11 tests nuevos (3 repo + 3 servicio + 5 router), 58 en total. Reviewer: APPROVED, todos los checkpoints superados. Sin migración nueva (people/credits ya existían en 0001).
2026-06-20 | feat_17 search_external_fallback | search/service.py fan-out paralelo (asyncio.gather) a TMDB/OpenLibrary/IGDB cuando 0 resultados locales; games/adapters/igdb.py añade search_games(); refresh_catalog_search movido a repository.py; 8 tests nuevos (139 total). Reviewer: APPROVED.
2026-05-24 | feat_5 games | games/models.py (Game, GameGenre, GamePlatform, Company, CompanyCredit, joins) + games/schemas.py (GameOut, GameGenreOut, GamePlatformOut) + games/repository.py (get_by_slug, upsert_game con genres/platforms/companies) + games/adapters/igdb.py (IGDBClient: OAuth2 Twitch client_credentials, token renewal con buffer 60s, get_game_by_slug, get_top_games, game_to_dict) + games/service.py (on-demand fallback) + games/routes.py (GET /games/{slug}) + main.py actualizado + migración Alembic 0005 (game_genres, games, game_genres_join, game_platforms, game_platforms_join, companies, company_credits, triggers) + 16 tests nuevos (4 repo + 3 servicio + 2 router + 7 cliente IGDB), 47 en total. Reviewer: APPROVED, todos los checkpoints superados. IGDB rating 0-100 normalizado a 0-10. game_type mapeado de int a string. first_release_date Unix timestamp → date.
2026-05-24 | feat_7 catalog_search | alembic/versions/0006_catalog_search.py (vista materializada UNION ALL de movies+series+books+games con tsvector + índice GIN) + backlogg/search/__init__.py + search/models.py (CatalogSearchEntry ORM read-only, PK compuesta slug+item_type) + search/repository.py (plainto_tsquery + ts_rank para ranking, filtro por item_type, paginación) + search/service.py + search/schemas.py (SearchResult, SearchResponse) + search/routes.py (GET /search, q requerido min_length=1 → 422, filtro type, paginación page/limit) + main.py actualizado + tests/test_search.py (8 tests: happy path, filtro tipo, exclusión, paginación, 422 sin q, 422 q vacío, sin resultados, campos esperados; fixture con REFRESH MATERIALIZED VIEW). 66 tests en total. Reviewer: APPROVED, todos los checkpoints C1-C22 aplicables superados.
2026-05-24 | feat_8 scheduler_sync | scheduler/__init__.py + scheduler/jobs.py (sync_movies, sync_series, sync_books, sync_games, cada uno con aislamiento de errores y REFRESH MATERIALIZED VIEW CONCURRENTLY al final) + scheduler/setup.py (AsyncIOScheduler con 4 CronTrigger: 02:00, 02:30, 03:00, 03:30 UTC) + admin/__init__.py + admin/schemas.py (SyncResponse Pydantic v2) + admin/router.py (POST /admin/sync/{type}, 202, response_model=SyncResponse, fire-and-forget via asyncio.create_task) + main.py (lifespan con scheduler start/stop, admin router registrado) + adapters: get_top_movies (TMDB /movie/popular), get_top_series (TMDB /tv/popular), get_trending_books (Open Library /trending/weekly) + tests/test_admin_sync.py (12 tests). 78 tests en total. Reviewer: APPROVED tras corrección de C10 (response_model Pydantic v2).
2026-05-25 | feat_10 nightly_sync_trigger | .github/workflows/nightly-sync.yml — cron 0 2 * * * + workflow_dispatch, llama a POST /admin/sync/{movie,series,book,game} usando secret RENDER_API_URL. Solución para Render free tier (servicio se apaga por inactividad). 78 tests en verde.
2026-05-24 | feat_9 deployment | Dockerfile (multi-stage: uv builder + python:3.12-slim runtime, usuario no-root, puerto 8080) + entrypoint.sh (alembic upgrade head → exec uvicorn) + .dockerignore + fly.toml (app=backlogg-api, región mad, 512MB shared CPU, auto-stop, force_https) + .github/workflows/ci.yml (push/PR a main, PostgreSQL 16 service, uv, bash init.sh). Sin cambios en backlogg/ ni tests/. 78 tests en verde. Reviewer: APPROVED.
2026-05-25 | feat_11 admin_stats | admin/repository.py (get_stats: 4 queries COUNT+MAX(last_synced_at)) + admin/service.py (get_stats → StatsResponse) + admin/schemas.py (ContentStats, StatsResponse Pydantic v2) + admin/router.py (GET /admin/stats delegando al servicio) + tests/test_admin_stats.py (6 tests). 84 tests en verde. Reviewer: APPROVED tras corrección de separación de capas (router sin imports SQLAlchemy).
2026-05-26 | feat_11 admin_stats (ext) | sync síncrono: scheduler/jobs.py retorna {"synced", "errors", "duration_s"} + admin/schemas.py SyncResponse actualizado (sin status, con synced/errors/duration_s) + admin/router.py (await handler, status_code=200, sin asyncio) + tests/test_admin_sync.py (patch.dict sobre _SYNC_HANDLERS) + nightly-sync.yml (--max-time 600, sin sleep). 84 tests en verde. Reviewer: APPROVED.
2026-05-26 | feat_13 catalog_search_unique_index | alembic/versions/0007_catalog_search_unique_index.py — CREATE UNIQUE INDEX uq_catalog_search_type_id ON catalog_search (item_type, id) para habilitar REFRESH MATERIALIZED VIEW CONCURRENTLY. downgrade con DROP INDEX IF EXISTS. 87 tests en verde. Reviewer: APPROVED.
2026-06-20 | feat_14 list_endpoints | movies/series/books/games: añadidos *SortEnum, *ListItemOut, *ListOut en schemas; list_* en repository (join géneros, filtro por slug, ORDER BY enum, COUNT total, offset/limit) y service (proyección ORM→Pydantic, mapeo first_air_date/first_publish_date→release_date); GET "" en cada router. 4 test files (24 tests nuevos), 111 tests en verde. Sin migración. Reviewer: APPROVED.
2026-06-20 | feat_15 genres_endpoint | backlogg/genres/ (módulo nuevo: __init__.py, schemas.py, repository.py con UNION ALL de 4 subqueries, service.py, routes.py) + main.py actualizado (genres_router registrado) + tests/test_genres.py (10 tests: all types, filtro movie/series/book/game, 422 invalid, campos response, counts reales). 121 tests en verde. Reviewer: APPROVED.
2026-06-20 | feat_16 similar_items | movies/adapters/tmdb.py + series/adapters/tmdb.py (get_*_recommendations via TMDB /recommendations) + schemas SimilarMovieOut/SimilarMoviesOut + SimilarSeriesOut/SimilarSeriesListOut + service get_similar_movies/get_similar_series (lookup external_id TMDB, fetch recomendaciones, persiste nuevos, devuelve top-10) + routes GET /movies/{slug}/similar y GET /series/{slug}/similar (declarados ANTES de /{slug}) + 10 tests de servicio. 131 tests en verde. Reviewer: APPROVED.
2026-06-21 | feat_20 trending_popular | trending/__init__.py + schemas.py (ItemTypeEnum, PeriodEnum, TrendingItemOut, TrendingOut) + service.py (TMDB trending fan-out paralelo para movie+tv, persist via upsert_movie/upsert_series, top 20) + router.py (GET /trending con type y period) + movies/adapters/tmdb.py get_trending_movies + series/adapters/tmdb.py get_trending_series + main.py actualizado + 11 tests nuevos. 169 tests en verde. Reviewer: APPROVED.
2026-07-04 | fix nightly-sync-workflow (PR #39) | Las runs nocturnas fallaban 10+ días: 4 jobs paralelos saturaban la instancia free de Render (books → error HTTP, movies → timeout 600s). Workflow reescrito: 1 job secuencial + wake-up /health + max-time 1800 + retries idempotentes. Run manual: success 28m14s.
2026-07-05 | refactor remove-inprocess-scheduler (PR #40) | Eliminado APScheduler in-process (código muerto: la instancia free duerme y los CronTrigger nunca disparaban; el sync real es GitHub Actions). scheduler/jobs.py conservado (lo usan /admin/sync/{type}). Paso verify del workflow endurecido: falla si algún last_synced_at > 2h. Docs actualizadas. Reviewer: APPROVED.
2026-07-05 | fix test-db-isolation (PR #41) | tests/conftest.py reescrito con guardia de aislamiento (aborta si TEST_DATABASE_URL falta, coincide con DATABASE_URL o database se importó antes), redirección global a backlogg_test + TRUNCATE inicial. tests/test_conftest_guard.py (6 tests). 218 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_23 seed_config_wiring | scheduler/jobs.py: 4 limit=100 hardcodeados sustituidos por settings.SEED_TOP_N_* (lectura en runtime) + tests/test_sync_seed_limits.py (4 tests con monkeypatch). 222 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_24 sync_slice_cursor | Nueva tabla sync_cursors (migración 0008_sync_slice_cursor) + backlogg/scheduler/repository.py (get/set offset, upsert). Adapters con offset retrocompatible (TMDB→páginas de 20 con tope page 500, Open Library/IGDB offset nativo). jobs.py: sync por tramos de SYNC_SLICE_SIZE (default 200) con wraparound a 0 al agotar SEED_TOP_N_* o API corta; offset expuesto en POST /admin/sync/{type}. Docs (schema/api/external-apis) y 17 tests nuevos (cursor, adapters, wraparound, idempotencia). 239 tests en verde. Reviewer: APPROVED (1 ronda de cambios — rename de migración al patrón <revision>_<feature_name>).
2026-07-05 | feat_25 books_popular_source | open_library.py: get_trending_books → get_popular_books (GET /search.json?q=*:*&sort=readinglog con offset/limit nativos, fields=key,title,author_name,first_publish_year,cover_i,subject,isbn, paginación per_page=100) sustituyendo /trending/weekly.json (fuente capada a cientos de entradas); jobs.py sync_books actualizado; docs/external-apis.md con nota "Popular-books strategy"; tests renombrados + 1 nuevo de endpoint/params. 240 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_26 direct_backfill_sync | scripts/backfill_sync.py (CLI uv run, reutiliza sync_* de jobs.py vía getattr, bucle hasta wraparound del cursor o presupuesto de tiempo default 300min, aborta si synced=0+errors>0) + param opcional slice_size en los 4 jobs (None → settings.SYNC_SLICE_SIZE, capped por SEED_TOP_N_*-offset) + igdb.py get_top_games pagina >500 en bucle con throttle 0.3s (~3.3 req/s) + .github/workflows/backfill-sync.yml (workflow_dispatch: content_type choice + seed_top_n default 10000, secrets DATABASE_URL/TMDB_API_KEY/TWITCH_CLIENT_ID/TWITCH_CLIENT_SECRET, timeout 350min) + docs/verification.md sección "Backfill manual" + tests/test_backfill_sync.py (14 tests mockeados). 254 tests en verde. Reviewer: APPROVED.
2026-07-06 | fix sync-genre-slug-collision | Backfill de books corría "success" con 0 persistidos: colisión uq_book_genre_slug (subjects distintos → mismo slug) envenenaba la sesión (InFailedSQLTransactionError en cascada) y el cursor no avanzaba. Fix: lookup slug-first en _get_or_create_genre de books/games (+platform; movies/series ya lo hacían) + dedupe intra-libro por slug + aislamiento por item en los 4 jobs (commit por item + _rollback_quietly con expunge_all). 7 tests nuevos (IntegrityError real). 261 tests en verde. Reviewer: APPROVED.
2026-07-06 | fix ol-error-masking | Un 500 transitorio de Open Library se enmascaraba como [] → sync_books lo leía como fin de resultados → cursor wrappeado a 0 y backfill "success" con 0 libros (run 28799265814). Fix: _fetch_popular_page con retry 5xx (3 intentos, backoff 1s/2s), raise en 4xx sin retry y en 5xx persistente; error a mitad de paginación descarta y lanza (cursor nunca wrappea por error). TMDB/IGDB verificados sin el patrón (ya usan raise_for_status). jobs.py y script sin cambios (sus guards ya hacían lo correcto al lanzar el adapter). 6 tests nuevos + 1 renombrado. 266 tests en verde. Reviewer: APPROVED.
2026-07-07 | docs docs/update-project-docs (PR #49) | README reescrito (quickstart, stack Render+GitHub Actions), docs/operations.md nuevo (runbook: nightly, backfill, admin endpoints, secrets, cursores), docs/api.md/architecture.md/verification.md actualizados al estado real. Merged.
2026-08-06 | backfill-catalog-complete | Confirmado via GET /admin/stats: movies=9371, series=9890, books=9976, games=10017, los 4 con last_synced_at=2026-08-06 (nightly diario). feat_26 (direct_backfill_sync) completó los 4 tipos: books ya estaba en 9974 desde el run 28827435184 (2026-07-06); movies/series/games se completaron en los dos runs del 2026-07-08 (28927353560 3h36m, 28927355351 1h47m) que no habían quedado documentados. Nightly sync corre sin fallos desde entonces (verificado hasta 2026-08-06). Backfill del catálogo: cerrado.
2026-08-09 | FE-7 testing_ci | Infraestructura de testing + CI del frontend (apps/web, Next 16.3), sin UI nueva. Vitest (`vitest.config.mts`, entorno jsdom, `resolve.tsconfigPaths` nativo de Vite 8) + Testing Library con 2 suites reales (`src/lib/utils.test.ts::cn` y `src/components/ui/button.test.tsx`: render/click/a11y/asChild), 14 tests verdes. MSW (`src/test/msw/{handlers,server}.ts`, solo `msw/node`) mockea `GET /v1/movies` y `GET /v1/movies/{slug}` con fixtures tipadas contra `@backlogg/api-client`; test de integración (`api-client.msw.test.ts`) ejercita el cliente tipado REAL contra los mocks (lista/detalle/404/override) en vez de inventar un hook/componente nuevo fuera de scope. Playwright (`playwright.config.ts`, un proyecto chromium, `webServer` sobre `next dev`) con smoke `e2e/home.spec.ts` (`/en` 200+hero, `/` redirige a locale default). Nuevo job `frontend` en `.github/workflows/ci.yml` en paralelo al job `test` backend (mismos triggers): `pnpm install` → `gen:api` (uv+Python 3.12, introspección `app.openapi()` sin DB, detecta drift del cliente tipado antes de typecheck) → typecheck → lint → test → build → `playwright install --with-deps chromium` → e2e. `pnpm-workspace.yaml` deniega el build-script de `msw` (solo usan `setupServer`, no `setupWorker`). Sin tocar `backlogg/`/`tests/`/`alembic/`; `bash init.sh` intacto (688 tests). Reviewer: APPROVED (verificación de los 4 comandos + `bash init.sh` re-ejecutada de forma independiente, sin hallazgos). Cierra M0 del roadmap frontend (FE-1..FE-7 done) — siguiente etapa: M1 catálogo público (FE-8 home/landing). FIX post-QA manual (mismo commit): `pnpm --filter web e2e` fallaba en local con "Another next dev server is already running" siempre que el desarrollador ya tuviera `next dev` levantado — no era un fluke del sandbox del implementer/reviewer sino un bug real: el lock de un-solo-dev-server-por-proyecto de Next 16 (`.next/dev/lock`) bloquea un segundo `next dev` sin importar el puerto pedido, y el config apuntaba a un puerto fijo (3100) casi nunca coincidente. `playwright.config.ts` ahora lee `.next/dev/lock` (si existe y no es CI) y reutiliza ese puerto como `baseURL`/`PORT`, cayendo a 3100 si no hay lock; en CI (checkout limpio, sin lock) el comportamiento no cambia. Verificado en vivo: `pnpm --filter web e2e` pasa sin variables de entorno manuales con un dev server ajeno ya corriendo en 3123. FIX post-PR (#83, mismo commit): el job `frontend` real de GitHub Actions falló en el primer run — `Set up Node.js` fijaba Node 20 a mano, pero `pnpm@11.20.0` (packageManager de la raíz) exige Node ≥22.13, así que `pnpm install` reventaba antes de nada (`ERR_UNKNOWN_BUILTIN_MODULE: node:sqlite`) con Node 20.20.2. Corregido: `ci.yml` pide Node 22 (satisface el mínimo real de pnpm) y `package.json` raíz (`engines.node`) actualizado de `>=20` (desactualizado) a `>=22.13` para que ambos queden consistentes con lo que pnpm realmente requiere. Verificado en local (Node 26.2.0) y `bash init.sh` intacto tras el cambio.

2026-08-09 | FE-6 pwa_baseline | PWA base instalable en apps/web (Next 16.3, Turbopack). `manifest.ts` en la raíz de `app/` (fuera de `[locale]`, para conservar el cacheo por defecto del archivo especial); iconos generados por código con `ImageResponse` (`icon.tsx`/`apple-icon.tsx` + Route Handler paramétrico `icons/[name]/route.tsx` para 192/512/512-maskable) — sin binarios a mano, no había assets de marca en el repo. Service worker vía Serwist: `@serwist/turbopack` (no `@serwist/next`, que no soporta Turbopack) + `src/sw.ts` con `runtimeCaching: defaultCache` (NetworkFirst/NetworkOnly para documentos/RSC/`/api/*`, CacheFirst/StaleWhileRevalidate solo para estáticos — no pisa el futuro SSR/ISR de M1) + `fallbacks.entries` sirviendo `/es/offline` o `/en/offline` en navegaciones fallidas; `ServiceWorkerProvider` (`SerwistProvider`) montado en `[locale]/layout.tsx`. Fix incidental (mismo commit): el matcher de `proxy.ts` (FE-4/FE-5) excluía del enrutado de locale solo paths con punto, redirigiendo 307 las rutas de iconos sin extensión (`/icon`, `/icons/icon-192`) a `/en/icon` y rompiendo el manifest — añadidas `icon`/`apple-icon` a la exclusión, sin tocar auth/refresh ni el resto del locale routing. Pipeline verde (`pnpm --filter web typecheck && lint && build`, exit 0) re-ejecutado de forma independiente por el reviewer, más QA manual contra `next build && next start` real (manifest/iconos/SW/offline pages 200, protección de rutas privadas intacta). Reviewer: APPROVED (1 hallazgo cosmético no bloqueante: comentario desactualizado en `sw.ts`). Sin tests automatizados nuevos (no existe runner en apps/web todavía — alcance de FE-7).
2026-08-06 | feat_27 users_auth | Arranca el épico social (auth + ratings/reviews + follows + feed; feature_list.json ampliado 27-30, CLAUDE.md/architecture.md/api.md actualizados — solo mensajería directa queda fuera de scope). backlogg/users/ (models, schemas, repository, service, auth con JWT/argon2, routes) + migración 0009_users.py + settings JWT_SECRET_KEY/JWT_ALGORITHM/JWT_EXPIRE_MINUTES + pyproject.toml (argon2-cffi, pyjwt) + main.py actualizado. Endpoints: POST /auth/register, POST /auth/login, GET/PATCH /users/me, GET /users/{username} (perfil público sin email). 39 tests nuevos, 305 en total. Reviewer: APPROVED (22 checkpoints, 1 observación no bloqueante: create_access_token no valida JWT_SECRET_KEY vacío — pendiente de configurar en Render antes de desplegar, igual que ADMIN_API_KEY).
2026-08-06 | feat_28 ratings_reviews | backlogg/ratings/ (models: UserRating + ReviewLike con UniqueConstraint polimórfico item_type/item_id estilo credits/external_ids; schemas; repository con upsert ON CONFLICT, recalculate_item_aggregates, like idempotente ON CONFLICT DO NOTHING; service; routes) + migración 0010_ratings_reviews.py (down_revision 0009) + endpoints añadidos a movies/series/books/games routes.py (PUT/DELETE /{slug}/rating, GET /{slug}/ratings con like_count) + POST/DELETE /ratings/{id}/like + GET /users/{username}/reviews (UNION ALL cross-type) + docs/schema.md y docs/api.md actualizados. Recalcula rating_internal (AVG)/rating_count_internal (COUNT) en el item afectado tras cada create/update/delete. 52 tests nuevos, 357 en total. Reviewer: APPROVED, 22 checkpoints + 12 criterios de acceptance verificados contra código (incluye C11: POST/DELETE /ratings/{id}/like usa id numérico en vez de slug, excepción deliberada documentada — una review no tiene slug natural). Ampliación post-QA manual: GET /{tipo}/{slug} no exponía rating_internal/rating_count_internal (se persistía pero no era visible vía API) — añadidos a MovieOut/SeriesOut/BookOut/GameOut y propagados desde el ORM en los 4 service.py; docs/api.md actualizado. Reviewer rechazó la primera vuelta (tests solo cubrían el caso None/0, sin probar la propagación real de un valor puntuado); corregido con 4 tests PUT rating + GET detail (uno por dominio) verificando el valor exacto. 361 tests en total. Reviewer: APPROVED en tercera ronda.
2026-08-07 | feat_29 follows | backlogg/follows/ (módulo nuevo: models Follow con UniqueConstraint(follower_id, followed_id)=uq_follow_pair, FKs a users ON DELETE CASCADE, índices idx_follows_follower/followed; schemas FollowUserOut/FollowListOut; repository con create_follow_if_not_exists (INSERT ON CONFLICT DO NOTHING → idempotente), delete_follow_if_exists (no-op tolerante), count_followers/following, list_followers/following paginados; service con self-follow 422 y target inexistente 404; routes bajo /users) + migración 0011_follows.py (down_revision 0010) + backlogg/users/schemas.py (UserOut extendido con follower_count/following_count) + backlogg/users/service.py (get_user_profile calcula counts vía follows.repository) + main.py (follows_router registrado) + docs/schema.md y docs/api.md actualizados. Endpoints: POST/DELETE /users/{username}/follow (auth, 204, idempotentes), GET /users/{username}/followers y /following (públicos, paginados). 32 tests nuevos (repo/service/routes: self-follow 422, doble follow idempotente, unfollow tolerante, 401/404, paginación, counts en perfil), 393 en total. Reviewer: APPROVED (22 checkpoints).
2026-08-08 | feat_30 activity_feed | Nuevo dominio backlogg/feed/ (repository.py único con SQLAlchemy, service.py, schemas.py FeedAuthorOut/FeedItemOut/FeedEntryOut/FeedListOut, routes.py feed_router prefix /feed) + GET /feed?tab=following|popular&page=&limit= (auth requerida, 401 sin token, 422 tab inválido) + UNION ALL cross-type sobre ITEM_MODELS con JOIN a users (autor sin N+1) y LEFT JOIN a subquery agregada de ReviewLike + coalesce(0) para like_count; following filtra por subquery embebida a follows (caller sin follows → vacío), popular ventana 30d ordenada por like_count desc/created_at desc; sin tablas ni migraciones nuevas + main.py registra feed_router + docs/api.md documentado + tests/feed/ (repo + endpoint). 405 tests en verde. Reviewer: APPROVED.
2026-08-08 | feat_31 user_library | Nuevo dominio backlogg/library/ (models LibraryEntry con status VARCHAR(20)+CHECK [want/in_progress/completed/dropped] y UniqueConstraint(user_id,item_type,item_id)=uq_library_entry_item, patrón polimórfico estilo ratings/credits; schemas LibraryStatus/LibraryTypeFilter/LibraryEntryIn/LibraryStatusOut/LibraryItemOut/LibraryEntryOut/LibraryListOut/LibraryCounts; repository con upsert ON CONFLICT DO UPDATE, get/delete, listado cross-type UNION ALL estilo feed/genres con release_date por tipo (movie→release_date, series→first_air_date, book→first_publish_date, game→release_date), counts zero-filled; service; routes) + migración 0012_user_library.py (down_revision 0011, reusa trigger_set_updated_at) + PUT/DELETE /{type}/{slug}/library en los 4 tipos (auth, 422/404) + GET /users/{username}/library (público, paginado, filtros ?status=/?type=) + viewer_status en los 4 GET detalle (nuevo get_current_user_optional en users/auth.py, auth opcional que nunca lanza 401; refactor books.get_book para construir BookOut explícito) + library_counts en GET /users/{username} (UserOut) + docs/schema.md y docs/api.md + bruno/Library/ (11 requests) + tests/library/ (40 tests). 445 tests en verde. Reviewer: APPROVED (22 checkpoints + acceptance verificadas). QA manual confirmado por el usuario.
2026-08-08 | feat_32 user_lists | Nuevo dominio backlogg/lists/ (models UserList [id, user_id FK→users CASCADE, slug, title, description?, is_public bool default true, timestamps; UniqueConstraint(user_id,slug)=uq_user_list_slug] y ListItem [list_id FK→user_lists CASCADE, item_type, item_id, position, added_at; UniqueConstraint(list_id,item_type,item_id)=uq_list_item]; schemas ListItemType/ListCreate/ListUpdate/ListItemRef/ListReorder/ListItemOut/UserListOut/UserListSummary/UserListsOut; repository única frontera SQLAlchemy reusando ITEM_MODELS/get_item_by_slug de ratings, resolución cross-type UNION ALL ordenada por position estilo library; service; routes lists_router /lists + user_lists_router /users/{username}/lists) + migración 0013_user_lists.py (down_revision 0012, reusa trigger_set_updated_at). Endpoints: POST /lists (201, slug derivado), PATCH/DELETE /lists/{slug} (owner 403/404), POST/DELETE /lists/{slug}/items ({item_type,slug}, idempotente, append + re-empaquetado de posiciones), PUT /lists/{slug}/items/order (valida set exacto → 422), GET /lists/{slug} (pública auth-opcional, privada 404 no-owner, items resueltos en orden), GET /users/{username}/lists (públicas siempre, privadas solo owner vía get_current_user_optional). Decisiones: colisión de slug con unicidad GLOBAL (sufijo -2/-3…, superconjunto de la constraint per-user, permite que /lists/{slug} resuelva a una única lista y distinga 404/403); slug no cambia en PATCH; payloads de items/order por {item_type,slug}; bug max(position) or -1 (0 falsy) corregido a check explícito de None. docs/schema.md y docs/api.md actualizados. 43 tests nuevos (repo/service/routes), 488 en verde. Reviewer: APPROVED (22 checkpoints + 12 criterios de acceptance). QA manual pendiente de confirmación del usuario.
2026-08-08 | feat_33 notifications | Nuevo dominio backlogg/notifications/ (models Notification con type enum [new_follower, review_like] + target polimórfico + índice (recipient_id, created_at desc), repository/schemas/service/routes) + migración 0014_notifications + wiring de generación en follows/service (new_follower solo en follow nuevo, no self) y ratings/service (review_like solo en like nuevo, autor != liker) vía RETURNING id para detectar inserción real + degradación elegante (commit del origen primero, helper try/except+rollback) + GET /notifications, GET /notifications/unread_count, POST /notifications/read + docs/schema.md y docs/api.md. Además fix del harness de tests preexistente: fixture db en tests/conftest.py migrado a patrón "transacción externa + SAVEPOINT" (join_transaction_mode=create_savepoint) que elimina la flakiness (~1/6) al hacer reversibles los commits de los handlers. 511 tests en verde (15/15 corridas). Reviewer: APPROVED.
2026-08-08 | feat_34 personalized_recommendations | Nuevo dominio solo-lectura backlogg/recommendations/ (schemas, repository, service, routes) + GET /recommendations (auth, ?type= con 422, page/limit). Semillas: ratings score>=4 + library completed/want (weight+source, dedup por (type,id)). Candidatos: solapamiento de género local (todos los tipos) + fan-out externo a similar de movies/series (feature 16) SOLO si faltan locales (gating verificado con assert_not_called). Exclusión de vistos (rated+library), reason legible, fallback populares por rating_external cuando no hay semillas. Sin modelo ni migración. Router en main.py, docs/api.md actualizado. 10 tests nuevos (7 servicio + 3 endpoint), 521 en verde. Reviewer: APPROVED.

- **Feature 35 · auth_refresh_tokens** (2026-08-08) — Access token corto (JWT 15 min) + refresh opaco persistido y rotatorio con revocación y detección de reuse. Modelo RefreshToken (solo hash sha256), migración 0015, POST /auth/login (par), /auth/refresh (rotación), /auth/logout (revoca idempotente). Reuse → 401 + revocación de cadena. Reviewer: APPROVED. 532 tests verdes.2026-08-08 | feat_36 account_recovery | Verificación de email y reset de password con tokens de un solo uso (tabla account_tokens: hash sha256, purpose verify/reset, expires_at, consumed_at — nunca en claro) + migración 0016 (down_revision 0015, users.email_verified bool default false). Interfaz EmailSender aislada (backlogg/core/email.py): transporte SMTP genérico de la stdlib (SmtpEmailSender: smtplib + EmailMessage, starttls/login condicionales, send_message en asyncio.to_thread) cuando SMTP_HOST está configurado, LoggingEmailSender fallback que loguea el enlace si no. SIN dependencias nuevas (se descartó Resend a mitad: dep resend eliminada). Endpoints POST /auth/verify/request (auth), /auth/verify/confirm, /auth/password/forgot (202 siempre, sin enumeración), /auth/password/reset (cambia hash + revoca refresh tokens de feat_35). Single-use + expiración → reuso 400; fallo SMTP no filtra ni rompe (degradación elegante, token commiteado antes de enviar); SMTP_PASSWORD nunca en logs. Settings SMTP_HOST/PORT/USERNAME/PASSWORD/FROM_EMAIL/STARTTLS + APP_BASE_URL + EMAIL_VERIFY_EXPIRE_HOURS/PASSWORD_RESET_EXPIRE_HOURS. Docs (schema/api/operations/external-apis, nota Gmail App Password para pruebas y dominio propio a futuro) + .env.example + 5 requests bruno + 24 tests. 556 tests en verde. Reviewer: APPROVED. QA manual con SMTP real (servidor de captura local): verify/reset end-to-end, no-enumeración confirmada en el transporte (0 correos al email inexistente).

2026-08-08 | feat_37 rate_limiting | Nueva capa core backlogg/core/rate_limit.py (RateLimiter ABC + InMemoryRateLimiter sliding-window-log con reloj inyectable y reset(); RateLimitResult; parse_rate "10/60"; get_rate_limiter() singleton — swap a Redis toca solo la factory; client_ip con X-Forwarded-For; dependency rate_limit_auth + enforce_search_fallback; detail 429 estático sin filtrar IP/límites, solo Retry-After variable). Wiring: dependencies=[Depends(rate_limit_auth)] SOLO en POST /auth/login y /register (refresh/logout/verify/reset intactos); gating del fan-out externo de /search dentro de service.py justo antes del fan-out y SOLO cuando total==0 (queries con resultados locales no consumen cupo). Settings RATE_LIMIT_AUTH/DEFAULT/SEARCH_FALLBACK (defaults generosos). conftest: fixture autouse reset del limiter por test (los tests comparten IP testclient). Docs api/operations + .env.example. 26 tests nuevos (unit limiter+parse_rate, endpoint auth 429+reset tras ventana con reloj falso, search fallback), 581 en verde. Reviewer: APPROVED (8 acceptance + checkpoints verificados contra código).

2026-08-08 | feat_39 metrics_endpoint | GET /metrics en formato de exposición Prometheus v0.0.4, stdlib-only (sin prometheus-client ni deps nuevas, coherente con feat_38/feat_36). backlogg/core/metrics.py (MetricsRegistry in-process thread-safe con un threading.Lock, singleton get_metrics() reemplazable estilo get_rate_limiter, serializador manual del texto de exposición, MetricsMiddleware) + backlogg/metrics/routes.py (router fino, PlainTextResponse content-type text/plain; version=0.0.4). Familias: http_requests_total{method,path,status}, http_request_duration_seconds{method,path} (histograma con buckets acumulativos _bucket{le}/_sum/_count), backlogg_syncs_total{type} (incrementado en los 4 sync jobs de scheduler/jobs.py), backlogg_external_fanout_total{source} (incrementado en los 4 _ingest_* del fan-out externo de search/service.py). Label path = PLANTILLA de ruta (request.scope["route"].path → /movies/{slug}), nunca la URL real; 404 → __unmatched__; sin query strings/headers/PII en labels. /metrics excluido de su propia instrumentación (evita ruido auto-referencial); grabación en finally para contar también los 500. MetricsMiddleware añadido antes de RequestIDMiddleware (RequestID sigue siendo el más externo; CORS/SecurityHeaders intactos). Sin settings nuevos (endpoint siempre activo). docs/api.md + docs/operations.md + bruno/Metrics/. 15 tests nuevos (registro/buckets/escapado/HELP-TYPE, endpoint 200+content-type+parseable, plantilla-no-URL, __unmatched__, exclusión propia, no-regresión, incremento real de contadores de negocio, no-PII), 614 en verde. Reviewer: APPROVED (22 checkpoints aplicables + 8 puntos de atención verificados contra código).

2026-08-08 | feat_41 openapi_polish | Enriquecido el esquema OpenAPI generado (solo metadatos, sin cambios de contrato: paths/métodos/status/response_model/campos intactos). backlogg/main.py: FastAPI ahora declara version="0.1.0" (alineado con pyproject.toml), description multilínea (constante API_DESCRIPTION: catálogo unificado + capa social, nota auth Bearer y admin X-API-Key) y openapi_tags=OPENAPI_TAGS (constante de módulo, 19 dominios con {name,description}; orden de include_router sin tocar). summary= conciso en cada decorador de ruta de los 18 routers + description= donde aporta (fallback on-demand, auth req/opcional, idempotencia, rate limiting, no-enumeración, owner-only). Ejemplos json_schema_extra (ConfigDict Pydantic v2, combinado con from_attributes donde existía) en schemas de respuesta clave: MovieOut, MovieListOut, SearchResponse, GenreListOut, TrendingOut, FeedListOut, LibraryStatusOut, LibraryListOut, UserListOut, UserListsOut, NotificationListOut, RecommendationsOut. tests/test_openapi_schema.py (6 tests): OpenAPI 3.x, info title/version/description, 19 tags con descripción, summaries en operaciones clave (GET /movies/{slug}, POST /auth/login, GET /feed, GET /search, GET /recommendations, POST /lists), presencia de example en components.schemas. 638 tests en verde. Reviewer: APPROVED (6 acceptance + checkpoints verificados; nota no bloqueante: el test de examples exige "al menos uno", podría endurecerse).

2026-08-08 | feat_42 account_deletion | DELETE /users/me (204, auth requerida) — borrado de cuenta GDPR. backlogg/users/routes.py (endpoint registrado ANTES de GET /users/{username}) + users/service.py delete_current_user (captura items rateados → borra fila usuario con flush → recomputa agregados → commit atómico) + users/repository.py delete_user + ratings/repository.py get_distinct_rated_items_for_user. SIN migración: los 8 FKs a users.id ya son ON DELETE CASCADE (user_ratings, review_likes, follows×2, library_entries, user_lists, notifications×2, refresh_tokens, account_tokens). El recompute corre SQL fresco tras el flush, no ve las filas borradas. docs/api.md + 2 requests bruno + 7 tests (401 sin/invalid token, 204+perfil 404, re-registro con mismo username/email, refresh 401 tras borrado, cascada verificada sobre las 8 tablas + datos de otro usuario sobreviven, recompute 3.0/2→2.0/1). 645 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev, server vivo): 11/11 checks incl. recompute count→0/rating→None, refresh 401, access token inválido tras borrado, username/email liberados.

2026-08-08 | feat_43 review_reports | Nuevo dominio backlogg/reports/ (models ReviewReport, schemas ReportStatus StrEnum/ReportReasonIn/ReportOut/ReportListOut, repository, service, routes con reports_router de usuario + admin_reports_router propio prefix /admin con Depends(verify_api_key)) + migración 0017_review_reports (down_revision 0016; tabla review_reports: reporter_id FK→users.id CASCADE, rating_id FK→user_ratings.id CASCADE, reason VARCHAR(300) nullable, status VARCHAR(20) CHECK(open/resolved) default open, created_at/resolved_at, UniqueConstraint(reporter_id,rating_id)=uq_review_report_pair, idx_review_reports_status). Endpoints: POST /reviews/{id}/report (auth; 201 primera vez / 200 idempotente mismo (reporter,review) preservando reason; body opcional; 404 review inexistente vía ratings.get_rating_by_id; 401 sin token), GET /admin/reports?status=&page=&limit= (X-API-Key, filtro estado + paginación, recientes primero), POST /admin/reports/{id}/resolve (X-API-Key, sella resolved+resolved_at, 404 inexistente). main.py registra ambos routers + tag OpenAPI reports. docs/schema.md + docs/api.md + 4 requests bruno + 24 tests (repo 6, service 6, routes 12). 669 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev migrada a 0017, server vivo): 13/13 incl. idempotencia (mismo id/reason preservado), body vacío, filtro/resolve admin, 401 sin key, y cascada rating_id (borrar la review elimina su reporte).

2026-08-08 | feat_44 content_moderation | Migración 0018 (down_revision 0017): user_ratings.is_hidden y users.is_banned (ambos NOT NULL DEFAULT false). Nuevo slice backlogg/moderation/ (schemas ReviewModerationOut/UserModerationOut, service set_review_hidden/set_user_banned, admin_moderation_router prefix /admin con verify_api_key — sin tabla propia, orquesta ratings.repository + users.repository). Condición de visibilidad reutilizable ratings.repository.visible_review_filters() (review visible sólo si is_hidden=false Y autor is_banned=false; requiere JOIN a users) aplicada de forma coherente en recalculate_item_aggregates (AVG/COUNT), list_ratings_for_item (count+paginada), list_user_reviews (cada rama del UNION ALL) y feed (list_following_feed + list_popular_feed). Recompute: hide/unhide recomputa el item de la review; ban/unban captura los items del usuario con get_distinct_rated_items_for_user (reusado de feat 42) y recomputa cada uno, commit atómico. Enforcement auth: usuario baneado → 401 genérico en login_user y en refresh_tokens (no revela el baneo, coherente con no-enumeración; en refresh el token no se rota). Endpoints admin (X-API-Key, idempotentes, 404 si no existe): POST /admin/reviews/{id}/hide|unhide (200 {id,is_hidden}), POST /admin/users/{username}/ban|unban (200 {username,is_banned}). main.py registra el router. docs/schema.md + docs/api.md + 5 requests bruno + 19 tests (17 endpoint + 2 repo). 688 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev migrada a 0018, server vivo): 18/18 — hide/ban excluyen de ratings+user reviews y recomputan agregado (cnt→0), login/refresh baneado 401, y unhide/unban restauran review y agregado.

2026-08-08 | feat_45 api_versioning | Barrido final: TODA la superficie de negocio bajo /v1. main.py aplica prefix="/v1" en los 23 include_router de negocio (movies, series, books, games, people, search, admin, genres, trending, auth, users, ratings, user_reviews, follows, feed, user_library, lists, user_lists, notifications, recommendations, reports, admin_reports, admin_moderation); cada router ya traía su prefix relativo → /v1/movies, /v1/auth/login, /v1/admin/reports, etc. Corte limpio SIN alias en la raíz. Exceptuados (ops, sin versionar): @app.get("/health") y metrics_router (/metrics). Ajuste interno obligado: backlogg/core/http_cache.py clasifica respuestas por route template (request.scope["route"].path), que ahora incluye el prefijo → DETAIL/LISTING/PRIVATE_TEMPLATES y _listing_max_age() actualizados a /v1/... (si no, la política de Cache-Control/ETag habría dejado de aplicar en silencio). NO se tocó users/service.py::_link (enlaces de frontend verify-email/reset-password sobre APP_BASE_URL, no endpoints de API). docs/api.md (79 paths + nota de política), docs/architecture.md (5), docs/operations.md (12) alineados; bruno 141 .bru con {{baseUrl}}/v1/..., Health/Metrics intactos. Tests actualizados a /v1 (incluidas plantillas en tests de metrics y paths en el test de OpenAPI de feat 41). 688 tests en verde. Reviewer: APPROVED. QA manual (leader, server vivo): 18/18 — /health y /metrics sin versionar, negocio bajo /v1, rutas raíz viejas → 404, openapi.json con paths /v1 y /health sin versionar. Con esto se cierra el backend inicial (features 1–45); siguiente etapa: frontend Next.js + React + TS consumiendo /v1.

2026-08-09 | FE-2 api_client | Nuevo paquete de workspace packages/api-client (@backlogg/api-client, private, type:module): cliente API tipado del frontend generado desde el OpenAPI del backend. openapi.json (63 paths, 61 bajo /v1) exportado con app.openapi() (introspección de rutas, sin DB) y committeado como input del generador; src/schema.d.ts generado por openapi-typescript 7.13.0 (committeado); src/index.ts con factory createApiClient(baseUrl, options?) sobre openapi-fetch tipada con `paths` (baseUrl NO hardcodeada, es argumento) + re-exports (paths, components, operations, webhooks, ClientOptions, Client, ApiClient, CreateApiClientOptions); src/example.ts fetch tipado GET /v1/movies que accede a campos reales (total:number, items[0].title:string) sin any/@ts-ignore, de modo que un drift del schema rompe el build (sanity check negativo verificado: forzar tipo erróneo → TS2322). Script gen:api = scripts/gen-api.mjs (Node): ejecuta `uv run python -c '...app.openapi()...'` desde la raíz, valida paths antes de sobrescribir, escribe openapi.json y corre openapi-typescript → src/schema.d.ts; README documenta cómo regenerar y re-committear. Wiring pipeline: @backlogg/api-client añadido como dep workspace:* de web y typecheck raíz pasa a `pnpm -r typecheck` (cubre ambos paquetes); web aún no importa el paquete (build de Next intacto, sin arrastrar nada a SSR). Verificación en verde: pnpm install, gen:api determinista (openapi.json+schema.d.ts sin diff al re-ejecutar), typecheck api-client, typecheck+lint+build web (Next 16.3), pnpm -r typecheck raíz. Sin tocar backlogg/, tests/ ni .env. Reviewer: APPROVED (4 acceptance + pipeline independiente verificada). Nota: bash init.sh/pytest del backend falla por Postgres local caído — irrelevante para esta feature frontend.

2026-08-09 | FE-3 design_system | Design system base del frontend (apps/web, Next 16.3 App Router + Tailwind v4). shadcn/ui inicializado con el CLI (components.json, `-b radix --preset nova`, rsc:true, CSS variables) → primitivos como CÓDIGO en el repo (src/components/ui/: button, input, card, dialog, label), no dep opaca. Utilidad src/lib/utils.ts (cn). Theming: tokens light/dark en src/app/globals.css (:root / .dark, dark por clase) + provider next-themes (src/components/theme-provider.tsx, "use client") montado en layout.tsx con suppressHydrationWarning y defaultTheme="system"+enableSystem (respeta prefers-color-scheme); toggle ModeToggle (Light/System/Dark, src/components/mode-toggle.tsx) reescrito sin setState-en-effect por la regla nueva de ESLint de Next 16. Página showcase interna en /showcase (prerenderizada estática) que renderiza los primitivos y el toggle. Ajuste incidental: reconciliadas las variables de fuente (--font-sans/--font-mono) entre layout.tsx y globals.css. Deps nuevas: next-themes, radix-ui, class-variance-authority, clsx, tailwind-merge, lucide-react, tw-animate-css; CLI shadcn movido por el leader de dependencies→devDependencies (no es runtime). Verificación en verde: pnpm --filter web typecheck && lint && build (PIPELINE_EXIT 0, rutas / _not-found /showcase). Scope no excedido (sin i18n/[locale], auth BFF, app shell ni PWA — features posteriores). Reviewer: APPROVED (4 acceptance + pipeline re-ejecutada). .env intacto.

2026-08-09 | FE-3b i18n | i18n del frontend (apps/web, Next 16.3 App Router) con next-intl@4.13.5 (locales en/es, default en, detección Accept-Language). Setup: src/i18n/{routing.ts (defineRouting locales:['en','es'], defaultLocale:'en'), navigation.ts (createNavigation → Link/redirect/usePathname/useRouter/getPathname), request.ts (getRequestConfig con requestLocale+hasLocale, carga ../../messages/${locale}.json)}; plugin createNextIntlPlugin() en next.config.ts (auto-detecta src/i18n/request.ts). CLAVE Next 16: middleware.ts está DEPRECADO/renombrado a proxy.ts → src/proxy.ts con createMiddleware(routing) y matcher '/((?!api|trpc|_next|_vercel|.*\\..*).*)' (build reporta ƒ Proxy (Middleware)). Reestructura app/ → app/[locale]/: nuevo root layout src/app/[locale]/layout.tsx (<html lang={locale}> + suppressHydrationWarning + vars Geist + generateStaticParams + generateMetadata vía getTranslations + setRequestLocale + notFound() para locale inválido + NextIntlClientProvider envolviendo ThemeProvider de next-themes), home migrada a page.tsx (server component, getTranslations('Home'), cero strings de UI hardcodeados), showcase movido a [locale]/showcase (copy en inglés como demo interna del design system, documentado). Eliminados src/app/{layout.tsx,page.tsx} viejos; globals.css/favicon.ico se quedan en src/app (import css → ../globals.css). Catálogos messages/{en,es}.json por namespaces (Metadata.home, Home, LanguageSwitcher), claves 100% consistentes entre ambos. Selector de idioma src/components/language-switcher.tsx (client, useLocale+useTranslations, router.replace(pathname,{locale}) preservando ruta). Ajustes de pipeline: package.json typecheck = 'next typegen && tsc --noEmit' (Next 16 genera tipos globales LayoutProps/PageProps en .next/types; typegen los crea sin build completo, tsc sigue comprobando todo); pnpm-workspace.yaml allowBuilds @parcel/watcher:false + @swc/core:false (coherente con sharp:false/unrs-resolver:false, política de no ejecutar build scripts nativos), completados tras el pnpm add obligatorio. Verificación en verde: pnpm --filter web typecheck && lint && build (exit 0, prerenderiza /en /es /en/showcase /es/showcase). Runtime (dev :3123): GET / con Accept-Language:es→307 /es, :en→307 /en; GET /en→200 <html lang="en"> h1 inglés; GET /es→200 <html lang="es"> h1 español. Reviewer: APPROVED (5 acceptance + pipeline re-ejecutada desde .next limpio, sin middleware.ts residual, claves en/es consistentes). Sin tocar backlogg/, tests/ ni .env. Nota: init.sh backend en verde al arranque (688 tests) — feature 100% frontend.

2026-08-09 | FE-4 auth_bff | Fundación del BFF de auth (apps/web, Next 16.3) con cookies httpOnly — el navegador nunca ve tokens. Route Handlers FUERA de [locale] en src/app/api/auth/{login,refresh,logout}/route.ts que proxan a ${API_INTERNAL_URL}/v1/auth/*: login setea DOS cookies httpOnly/Secure(prod)/SameSite=lax (bl_access maxAge=900s alineado a JWT_EXPIRE_MINUTES=15, bl_refresh maxAge=2592000s alineado a REFRESH_EXPIRE_DAYS=30) y responde {ok:true} sin tokens (401 invalid_credentials, 429 propaga Retry-After); refresh usa refreshSession() para rotar el par y REESCRIBIR ambas cookies (401 session_expired limpia cookies); logout revoca en /v1 (Bearer access + {refresh_token}) y limpia SIEMPRE ambas cookies (204 idempotente). Módulos lib: env.ts (server-only, API_INTERNAL_URL lazy), auth/cookie-names.ts (bl_access/bl_refresh, SIN server-only p/ proxy), auth/cookies.ts (opciones+read/set/clearAuthCookies), auth/session.ts (getApiClient lazy + authHeader + refreshSession factorizado, reusado por handler y apiFetch), auth/protected-routes.ts (PROTECTED_PREFIXES /settings,/library,/notifications,/lists,/feed,/recommendations + isProtectedPath, SIN server-only), api-fetch.ts (apiFetch<T> server-only: adjunta access, refresca 1 vez ante 401 y reintenta; getCurrentUser() memoizado con React cache() sobre /v1/users/me). proxy.ts envuelve createMiddleware(routing) de next-intl (chequeo OPTIMISTA solo lee cookie, sin red/DB; redirige rutas privadas a /${locale}/login respetando locale; matcher intacto excluye /api). apps/web/.env.example (API_INTERNAL_URL=http://localhost:8000, ORIGIN sin /v1) + excepción !.env.example en .gitignore. cookies() usado async (Next 16). NO se creó middleware.ts ni la página /login (FE-13); sin UI nueva → catálogos i18n intactos. Pipeline verde: pnpm --filter web typecheck && lint && build (exit 0). E2E verificado por el implementer contra backend real (docker backlogg-db + alembic upgrade head + uvicorn :8000 + next dev :3000, user fe4tester): login 200 setea cookies sin tokens en body, refresh rota bl_refresh, logout 204 borra cookies, logout sin sesión 204 idempotente, refresh sin cookie 401, malas creds 401, reuse de refresh revocado 401+cookies limpiadas, middleware /en/library→307 /en/login y /es/settings→307 /es/login, /en público 200. Reviewer: APPROVED (pipeline re-ejecutado por el reviewer, sin NEXT_PUBLIC, server-only en módulos con tokens). SEGUIMIENTO (hallazgo no bloqueante): safeSet/safeClear en session.ts tragan la escritura de cookie en render RSC read-only → si el access expira y se refresca DENTRO del render de un Server Component, el backend rota (revoca el refresh viejo) pero la cookie no se actualiza → siguiente request envía refresh revocado → backend lo trata como REUSE y revoca TODAS las sesiones. Hoy inofensivo (no hay página RSC protegida). RESOLVER antes/durante FE-5 (app_shell), primera feature que consumirá getCurrentUser en render: enrutar el refresh disparado desde RSC por Route Handler/Server Action que pueda persistir cookies. Cosmético: rama redundante en login/route.ts:48 (status = x===401?401:x). Sin tocar backlogg/ ni tests/; .env real intacto.
2026-08-09 | FE-9 browse_by_type | apps/web/src/app/[locale]/browse/[type]/page.tsx (Server Component, SSR/ISR, lee searchParams genre/sort/page/limit) + loading.tsx + browse-filters.tsx/browse-pagination.tsx (client, actualizan la URL vía query params, siempre pasan genre/sort explícitos para poder limpiar filtros) + catalog-types.ts (vocabulario puro, sin `server-only`, extraído de catalog.ts para no filtrar el import server-only a bundles cliente) + catalog.ts ampliado (listCatalog/getGenres, mismo patrón de FE-8: getApiClient() sin cookies, next:{revalidate}, try/catch → array vacío) + messages/{en,es}.json (namespace Browse) + MSW handlers/fixtures para genre/sort/page/limit + e2e/browse.spec.ts. Dos bugs reales atrapados y corregidos en la propia implementación (no solo en review): fuga de `server-only` a un client component vía import transitivo de catalog.ts, y filtro que no se podía limpiar en BrowseFilters.navigate() por `next.genre ?? selectedGenre` (no distinguía "limpiado" de "sin tocar"). Resuelve los enlaces "ver todo" de la home (FE-8) que apuntaban a /browse/{type}. Reviewer: APPROVED (re-ejecutó typecheck/lint/test 53-53/build/e2e 3-3 en verde, verificado contra docs/api.md, ambos fixes confirmados en código real). Nota cosmética no bloqueante: comentario stale en catalog-card.tsx menciona FE-9/FE-10 como pendientes. FIX post-QA manual del usuario (mismo commit): los `<select>` de género/sort eran ilegibles (texto blanco sobre fondo blanco) — `SELECT_CLASSNAME` en browse-filters.tsx copiaba `bg-transparent` de `ui/input.tsx` (FE-3), pero a diferencia de `<input>`, un `<select>` nativo no hereda `color`/`background-color` de forma fiable entre navegadores: el popup de `<option>`s se pinta según el `color-scheme` heurístico del SO/navegador cuando el elemento no declara uno propio, y si ese heurístico no coincide con el `.dark` del sitio (p.ej. SO claro + toggle del sitio en oscuro) el resultado es texto y fondo del mismo tono. Corregido fijando `bg-background`+`text-foreground` explícitos más `[color-scheme:light] dark:[color-scheme:dark]` en el `<select>`, y un nuevo `OPTION_CLASSNAME` (`bg-background text-foreground`) en cada `<option>` porque algunos navegadores (Safari) tampoco heredan color en el popup. Verificado visualmente en ambos temas (Playwright headless contra dev server real, screenshots + estilos computados). Pipeline re-verificado en verde.
2026-08-11 | FE-11 global_search (id=12, frontend_feature_list.json) | Búsqueda global cross-type bajo `/search`: input con debounce (400ms, comparado contra la URL actual para no navegar en montaje ni en cambios ajenos), filtro por `type`, paginación, estado en query params (`q`/`type`/`page`, shareable). Nuevo `apps/web/src/lib/search.ts` (`searchCatalog`, server-side, sin `revalidate` porque puede disparar fallback externo) contra `GET /v1/search`, con `SearchResult` discriminado (`ok`/`invalid`/`rate-limited`/`error`). Manejo de 429: `Retry-After` parseado literalmente del backend, `RateLimitNotice` hace countdown exacto y solo habilita un botón manual al llegar a 0 (nunca reintento automático). Estado vacío (sin query) y "sin resultados" (`results: []` real) distinguidos. Nuevos componentes `search-controls`, `rate-limit-notice`, `search-pagination` (no reutiliza `BrowsePagination` de FE-9 por distinto shape de query params) + tests Vitest para cada uno. i18n: namespace `Search` completo en es/en, cero strings hardcodeados. Nav del header enlaza a `/search` (la CTA de home de FE-8 ya apuntaba ahí). Reviewer APPROVED tras ejecutar typecheck/lint/test(111 verdes)/build él mismo; QA manual en navegador queda pendiente antes de mergear.
2026-08-11 | FE-12 genres_trending_browse (id=13, frontend_feature_list.json) | Dos páginas públicas nuevas bajo `apps/web/src/app/[locale]/`: `/genres` (GET /v1/genres con filtro `?type=`, géneros agrupados por tipo, cada uno enlaza a `/browse/{type}?genre=slug` de FE-9) y `/trending` (GET /v1/trending con `?type=`/`?period=`, reutiliza `CatalogCard` igual que home/browse). Ambas siguen el patrón SSR/ISR ya establecido por FE-8/FE-9 en `catalog.ts` (fetch vía `@backlogg/api-client`, `next.revalidate` a nivel de fetch). Nuevas `getGenrePage`/`getTrendingPage` con resultado explícito `{ ok: true/false }` (mismo patrón que `listCatalog` de FE-9) en vez de degradar a `[]` como las `getGenres`/`getTrending` existentes (que quedan intactas, sin riesgo de regresión en dropdown de browse ni sección trending de home). Filtros como `<select>` client component (mismo patrón que `BrowseFilters`/`SearchControls`). Nav del header con enlaces "Trending"/"Genres"; home gana un "view all" hacia `/trending`. i18n: namespaces `Genres`/`Trending` completos en es/en, cero strings hardcodeados. Reviewer APPROVED tras re-ejecutar typecheck/lint/test(132 verdes)/build él mismo, verificado contra docs/api.md y consistencia de patrones FE-8/FE-9/FE-11; sin cambios requeridos. QA manual en navegador queda pendiente antes de mergear.
2026-08-11 | FE-12 fix post-QA — navbar wrap por locale | El usuario detectó en QA manual que el header en español caía a 2 líneas en desktop mientras que en inglés se mantenía en 1 (Tendencias/Géneros/Iniciar sesión más largos que su equivalente en, y el contenedor capado a max-w-5xl no daba margen — confirmado con Playwright: persistía incluso a 1600px de viewport, no era un problema de breakpoint responsive sino overflow real). Fix en site-header.tsx: layout con breakpoint explícito (flex-col por debajo de xl=1280px, xl:flex-row desde ahí) en vez de flex-wrap orgánico reactivo al contenido, más max-w-5xl→max-w-6xl (verificado por el reviewer que coincide con la convención real de home/browse/genres/trending/search/item-detail — único desajuste pre-existente y no bloqueante: site-footer.tsx sigue en max-w-5xl, queda como seguimiento) y sm:px-10→sm:px-8. Verificado (implementer y luego reviewer de forma independiente) que la altura del header coincide exactamente entre en/es en 1600/1280/1024/900/768/640px, y que el apilado a <1280px se ve bien en ambos locales (capturas). typecheck/lint/test(132)/build en verde. Reviewer: APPROVED. Bundleado en la misma rama feat/fe12-genres-trending-browse, sin commit propio — va en el mismo commit que FE-12 al shippear.
2026-08-12 | FE-15 email_verification (id=16, frontend_feature_list.json) | Ruta `/verify-email?token=` (Server Component `app/[locale]/verify-email/page.tsx` leyendo `token` de `searchParams`, sin `useSearchParams`/`Suspense` — mismo patrón que `/search`) + `verify-email-status.tsx` (client, 5 estados: missing/loading/success/invalid(400)/error; guard `useRef` para disparar `POST /api/auth/verify/confirm` una sola vez, evitando que el doble-invoke de efectos de React 19 Strict Mode queme el token de un solo uso). Dos Route Handlers BFF nuevos bajo `app/api/auth/verify/`: `confirm` público (proxa `POST /v1/auth/verify/confirm`, el token del enlace es la credencial, sin sesión) y `request` protegido (usa `apiFetch` de FE-4 con auto-refresh, `Authorization: Bearer`). Acción "reenviar verificación" (`use-resend-verification.ts`, mismo esqueleto que `use-logout.ts`) colgada del menú de cuenta existente (`user-nav.tsx`, FE-14), visible solo si `!emailVerified` — sin crear página de ajustes nueva (eso es FE-17 `account_settings`, aún `pending`, explícitamente fuera de scope). `site-header.tsx` propaga `email_verified` al `NavUser`. i18n: namespaces `Auth.verifyEmail.*`/`Nav.resendVerification*`/`Metadata.verifyEmail` en es/en, cero strings hardcodeados. Sin test e2e Playwright nuevo (paridad con FE-13: todos los casos son mockeables con MSW, sin depender de cookies reales como sí necesitó FE-14). `APP_BASE_URL` no se tocó — ya documentado desde el backend (`account_recovery`, id 36, `done`). Pipeline verde: typecheck/lint/test(191)/build, re-ejecutado de forma independiente por el reviewer (más Playwright 7/8, el único fallo confirmado como flakiness preexistente del route-announcer en un spec no tocado por esta rama) y `bash init.sh` (690 tests backend intacto). Reviewer: APPROVED sin cambios requeridos. QA manual (6 pasos, en/es) queda pendiente de confirmación del usuario antes de shippear.
2026-08-12 | FE-16 password_recovery (id=17, frontend_feature_list.json) | Rutas BFF `app/api/auth/password/{forgot,reset}/route.ts` (mismo patrón proxy que `verify/{request,confirm}` de FE-15; `forgot` siempre 202 sin enumeración, `reset` propaga 200/400 invalid_token/422) + páginas `/[locale]/forgot-password` (form neutro, 202 y 422 tratados igual porque un 422 solo puede ser de formato, nunca de existencia del email) y `/[locale]/reset-password?token=` (Server Component lee `token` de `searchParams` sin `useSearchParams`, mismo patrón que `/verify-email`; `reset-password-status.tsx` difiere de `verify-email-status.tsx` en que renderiza un formulario en vez de disparar la acción al montar, porque hace falta la nueva password) + enlace "¿Olvidaste tu contraseña?" en `login-form.tsx` + `forgotPasswordSchema`/`resetPasswordSchema` en `schemas.ts` (zod, min 8, mirror del backend) + i18n completo `Auth.forgotPassword.*`/`Auth.resetPassword.*` en es/en. Sin auto-login tras reset (el backend revoca todos los refresh tokens, `docs/api.md`); BFF de `reset` no toca cookies locales a propósito. Sin manejo de 429 (confirmado en `backlogg/users/routes.py` que estas dos rutas no tienen `rate_limit_auth`, a diferencia de login/register). Sin e2e Playwright nuevo (todo mockeable con MSW, mismo criterio que FE-15). Pipeline verde: typecheck/lint/test(213)/build + `bash init.sh` (690 tests backend intacto), re-ejecutado de forma independiente por el reviewer, que además hizo QA en vivo end-to-end contra backend+DB real (docker compose): forgot con email inexistente y real → 202 ambos; token de reset obtenido con un script temporal (`FakeEmailSender`, ya que el logger de producción redacta cualquier texto que matchee `password:`); reset con password corta → 422, válido → 200, mismo token reutilizado → 400 invalid_token; login con password vieja → 401, nueva → 200. Reviewer: APPROVED sin cambios requeridos.
2026-08-12 | FE-17 account_settings (id=18, frontend_feature_list.json) | Página protegida `/[locale]/settings` con tres bloques: edición de perfil (`account-profile-form.tsx`, rhf+zod, `display_name`/`bio`/`avatar_url`; campo vaciado se envía como `null` para que el backend lo borre en vez de ignorarlo; feedback vía toast + `router.refresh()` para sincronizar el nav), estado de verificación de email (`account-verification-status.tsx`, reusa `useResendVerification()` de FE-15 sin reimplementar) y borrado de cuenta con doble confirmación real (`delete-account-dialog.tsx`: abrir diálogo + escribir el username exacto para habilitar el botón destructivo). BFF `app/api/users/me/route.ts` (`PATCH`/`DELETE` vía `apiFetch`; `DELETE` limpia cookies locales solo tras un 204 confirmado, sin llamar a `/v1/auth/logout` porque el backend ya invalida los refresh tokens en cascada al borrar la cuenta — asimetría justificada frente a `logout/route.ts`). `Textarea` nueva en `ui/` (no existía primitive multilínea). i18n completo namespace `Settings.*` + `Metadata.settings.*` en es/en. Ronda 1 del reviewer: `CHANGES_REQUESTED` — único bloqueante, `/settings` inalcanzable desde la UI (FE-14 había dejado el dropdown de cuenta explícitamente preparado para esto y no se usó). Fix mínimo: `DropdownMenuItem` en `user-nav.tsx` → `/settings`, string `Nav.settings` en ambos locales, test de comportamiento real (no snapshot). Ronda 2: `APPROVED`, verificado archivo a archivo de forma independiente por el reviewer. Pipeline verde: typecheck/lint/240 tests/build + `bash init.sh` (690 tests backend intacto, sin diff en `backlogg/`/`tests/`). Sin enlace de navegación previo a esta feature; ahora es la primera entrada del menú de cuenta.

2026-08-13 | FE-18 rate_review (id=19, frontend_feature_list.json) | Widget de rating 1-5 + reseña de texto montado en el detalle de item (`apps/web/src/components/rating-widget.tsx`, entre `ItemHero` y `ItemCredits`), crear/editar/borrar con UI optimista contra `PUT`/`DELETE /v1/{type}/{slug}/rating` vía nuevo BFF `app/api/[type]/[slug]/rating/route.ts` (GET/PUT/DELETE, validacion de score/review_text mirror de `RatingIn`). Boton "reportar" generico (`report-review-button.tsx`, solo pide `ratingId: number`, pensado para que FE-19 lo reutilice) contra `POST /v1/reviews/{id}/report` via `app/api/reviews/[id]/report/route.ts`, idempotente 200/201. Sin endpoint "mi rating" dedicado: el estado inicial se resuelve escaneando `GET /v1/{type}/{slug}/ratings?limit=100` por username del viewer (`lib/ratings.ts`), limitacion documentada si el rating propio cae mas alla de la primera pagina en un item muy popular. Agregados (`rating_internal`/`rating_count_internal`) recalculados localmente y sin refetch via `recomputeAggregate` (`lib/ratings-aggregate.ts`, funcion pura con tests de AVG/COUNT) para no romper la ventana ISR de la pagina de detalle; el widget hace su propio fetch client-side en `useEffect` (no `getCurrentUser()` en el Server Component) para que `/[locale]/[type]/[slug]` siga sin forzar `cookies()` — confirmado en el log de `next build`. i18n completo namespaces `ItemDetail.rating`/`ItemDetail.report` en es/en. Pipeline verde: typecheck/lint/310 tests (70 nuevos)/build + `bash init.sh` (690 tests backend intacto, sin diff en `backlogg/`/`tests/`/`alembic/`). Reviewer: APPROVED sin cambios requeridos. QA manual en navegador queda pendiente de confirmacion del usuario antes de shippear.
2026-08-13 | bugfix rating_aggregate_stale_isr (fuera de backlog, encontrado en QA manual del usuario tras aprobar FE-18) | "Backlogg rating" en el detalle de item (`rating-widget.tsx` y el chip estatico de `ItemHero`) seguia mostrando "No ratings yet" indefinidamente tras recargar la pagina pese a tener una rating real guardada (confirmado en DB y via `curl` directo a `GET /v1/movies/{slug}`, backend con `rating_internal: 4.0` correcto). Causa raiz confirmada en vivo (Playwright contra backend/DB reales, usuario de prueba, item nunca antes puntuado): `getItemDetail` (`apps/web/src/lib/catalog.ts`) cacheaba el fetch a `GET /v1/{type}/{slug}` con `next: { revalidate: ITEM_REVALIDATE_SECONDS }` (ISR, 1h) sin `tags`, asi que el `PUT`/`DELETE /api/{type}/{slug}/rating` nunca invalidaba esa cache. Fix: nuevo `itemDetailCacheTag(type, slug)` en `catalog.ts` anadido a `tags` del fetch, mas `revalidateTag(itemDetailCacheTag(type, slug), { expire: 0 })` en `PUT`/`DELETE` exitosos de `app/api/[type]/[slug]/rating/route.ts` (Next 16.3 exige el segundo argumento `profile`; `{ expire: 0 }` da invalidacion inmediata read-your-own-writes, a diferencia de `"max"` que solo da stale-while-revalidate, confirmado leyendo `node_modules/next/dist/docs/`). El widget de `RatingWidget` en si nunca tuvo el bug (su update optimista post-submit ya funcionaba bien sin recargar); el reporte original lo confundia con el chip estatico de `ItemHero`, que comparte la misma causa raiz de cache. Tests nuevos: `catalog.test.ts` (estabilidad de `itemDetailCacheTag`), `route.test.ts` (`revalidateTag` llamado solo en exito, nunca en 401/404/422/500), `rating-widget.test.tsx` (caso "primera puntuacion, sin recargar" + mock de `next-intl` mejorado para interpolar variables). Verificado en vivo antes/despues con Playwright (transcript en `progress/impl_bugfix_rating_aggregate.md`). Pipeline verde: typecheck/lint/321 tests/build + `bash init.sh` (690 tests backend intacto, sin diff en `backlogg/`/`tests/`/`alembic/`). Reviewer: APPROVED (veredicto en texto, sin archivo `progress/review_*` por ser bugfix fuera de backlog). Bundled en la misma rama/commit de ship que FE-18 (`feat/fe18-rate-review`).
2026-08-13 | refinamiento fe18_review_polish (fuera de backlog, feedback de QA manual del usuario sobre FE-18 ya done) | Estilo y fechas para la seccion "Your rating" de `RatingWidget` (unica superficie de reviews hoy; el listado de reviews ajenas es FE-19, pending). Fase `summary` ahora envuelta en `<Card><CardContent>` (`@/components/ui/card`, primitive shadcn ya existente, mismo lenguaje visual que `item-credits.tsx`/`item-hero.tsx`); `whitespace-pre-wrap` en el texto de la review. Nuevo `apps/web/src/lib/format-date.ts` (`Intl.DateTimeFormat`, `dateStyle: "long"`, `timeZone: "UTC"` fijo porque los timestamps del backend son UTC) usado con `useLocale()` de next-intl para mostrar `rating.created_at` siempre (`createdOnLabel`) y `rating.updated_at` solo si difiere de `created_at` (`editedOnLabel`, comparacion directa de strings ISO). i18n simetrico en/es bajo `ItemDetail.rating.*`. Sin cambios funcionales (editar/borrar/reportar identicos) ni fuera de scope (`item-hero.tsx`, `release_date`, fase `form` del widget, FE-19 intactos). Tests nuevos: `format-date.test.ts` (en/es, limite UTC 23:30) + 2 casos en `rating-widget.test.tsx` (editedOnLabel ausente/presente segun corresponda). Pipeline verde: typecheck/lint/326 tests/build + `bash init.sh` (690 tests backend intacto). Reviewer: APPROVED (veredicto en texto, sin archivo `progress/review_*` por ser refinamiento fuera de backlog). Nota: un intento previo de esta tarea se corto a mitad por un limite de gasto de API (dejo solo `format-date.ts` creado); se relanzo el implementer y completo el resto sin perdida de trabajo. Bundled en la misma rama/commit de ship que FE-18 (`feat/fe18-rate-review`).
2026-08-13 | refinamiento fe18_rater_identity (fuera de backlog, feedback de QA manual del usuario sobre FE-18 ya done) | Avatar + nombre del puntuador anadidos a la seccion "Your rating" de `RatingWidget` (junto a `StarRow`, fechas de creacion/edicion del refinamiento anterior intactas a la derecha). Nuevo componente `RaterIdentity({ user })` que recibe `rating.user` (`RatingAuthorOut`: `username`/`display_name`/`avatar_url`, ya presente en la respuesta del backend, sin tocar backend/BFF) como prop explicita en vez de leer de sesion/contexto — deliberado, para no asumir implicitamente "es siempre el usuario actual", ya que FE-19 (pending) reutilizara un patron similar para reviews ajenas. Reusa el lenguaje visual de `user-nav.tsx` (`<img>` con `avatar_url` o fallback de iniciales en circulo `bg-muted`, mismo comentario sobre por que no `next/image` — remotePatterns pendientes); `initials()` de `user-nav.tsx` no se reutilizo (no exportada, tipo camelCase vs snake_case de `RatingAuthorOut`) sino que se duplico localmente en 3 lineas, documentado en JSDoc — el reviewer lo acepto como valido aunque anoto que una firma mas simple (`initialsFromName(name)`) habria evitado el argumento del adaptador; observacion de estilo, no bloqueante. Nombre mostrado: `display_name ?? username`. 3 tests nuevos en `rating-widget.test.tsx` (avatar presente, fallback a iniciales sin avatar, fallback a username sin display_name). Pipeline verde: typecheck/lint/329 tests/build + `bash init.sh` (690 tests backend intacto). Reviewer: APPROVED (veredicto en texto, sin archivo `progress/review_*` por ser refinamiento fuera de backlog). Bundled en la misma rama/commit de ship que FE-18 (`feat/fe18-rate-review`).
2026-08-13 | bugfix issue_7_credits_vacios (tracker de bugs, issues_list.json id=7, recurrencia de Issue 1) | Investigacion previa (agente Explore) confirmo dos causas combinadas en `backlogg/scheduler/jobs.py` (`sync_movies`/`sync_series`/`sync_books`): (1) `TMDBClient`/`TMDBSeriesClient` sin timeout explicito (default httpx 5s) ni retry/backoff ante 429/5xx/timeout en ninguna llamada HTTP, asi que cualquier hipo transitorio de TMDB perdia el credits de ese item sin reintento; (2) el fallo de `_persist_movie_people`/`_persist_series_people`+`_persist_series_creators`/`_persist_book_authors` se traga en un `except Exception` que solo hace `logger.exception`+rollback, por diseno sin incrementar `errors` (acceptance criterion C19, test `test_sync_movies_persist_people_failure_does_not_increment_errors`), lo que lo dejaba invisible en `GET /admin/stats` (que ni expone un campo `errors`) y en el workflow nocturno (que descartaba el body de la respuesta). `sync_games` descartado como afectado: `get_top_games` trae `involved_companies` en la misma query bulk, sin 2a llamada HTTP por item. Fix: timeout 10s + retry con backoff exponencial (tenacity, 3 intentos, 1s/2s/4s, solo 429/5xx/timeout/transport, nunca 404 ni otros 4xx) en todos los metodos de red de `TMDBClient`/`TMDBSeriesClient` (`backlogg/movies|series/adapters/tmdb.py`, helper `_get_popular_page` extraido de `get_top_movies`/`get_top_series` para no reiniciar la paginacion completa en cada retry); nuevo contador `people_errors` (`SyncResponse.people_errors: int = 0`) en `sync_movies`/`sync_series`/`sync_books`, incrementado solo en el except que antes solo logueaba, sin tocar la semantica de `errors` (C19 intacto); `.github/workflows/nightly-sync.yml` actualizado para loguear `people_errors` como `::warning::` por tipo sin fallar el job (editado directamente por el leader, fuera de `backlogg/`/`tests/`). Tests nuevos: `tests/movies/test_tmdb_adapter.py` y `tests/series/test_tmdb_adapter.py` (retry exitoso, no-retry en 404, agotamiento de reintentos, no-retry en 4xx no-404); `tests/test_admin_sync.py` extendido con `people_errors==1`/`errors==0` para movies (existente) y nuevos equivalentes de series/books (no existian antes). `bash init.sh` en verde (702 tests). Reviewer: APPROVED en texto (sin `progress/review_*`, bugfix fuera de `feature_list.json`). QA manual del leader: DB local antes del fix 3/104 movies con credits; tras el fix, `uv run python scripts/backfill_sync.py movie --slice-size 15 --time-budget-minutes 1` (reutiliza `sync_movies` real) dejo 100/166 movies con credits, 0 errors, 0 people_errors; spot check del item citado en el sintoma (Spider-Man: Brand New Day, id=23) paso de 0 a 11 filas en `credits`. Pendiente: mismo backfill/verificacion en vivo para series/books (comparten el fix, no se corrio el backfill real de esos dominios en esta sesion) y confirmacion del usuario para commit/push/PR a `main` desde `fix/issue-7-credits-vacios`.
2026-08-13 | bugfix issue_8_nightly_sync_wrong_urls (tracker de bugs, issues_list.json id=8, reportado por el usuario en la misma sesion que Issue 7) | `gh run list --workflow=nightly-sync.yml` confirmo exito el 2026-08-09 (synced:100/errors:0 los 4 tipos) y fallo cada noche desde 2026-08-10 (~9min, exit code 22 en los 4 pasos de sync + verify stats). Causa confirmada con `gh run view --log-failed`: PR #73 (`feat/api_versioning`, commit 590ea86, 2026-08-09) monto `admin_router` bajo `/v1` (`backlogg/main.py:238`) dejando solo `/health`/`/metrics` sin versionar, pero `.github/workflows/nightly-sync.yml` seguia llamando a `/admin/sync/{type}` y `/admin/stats` sin el prefijo `/v1` -> 404 inmediato en Render (curl -f exit 22), mientras "Wake service" (`/health`, sin versionar) seguia en verde, lo que explicaba el patron. Fix: prefijadas con `/v1` las 5 URLs de `/admin/*` en el workflow; `.github/workflows/backfill-sync.yml` confirmado sin impacto (no usa HTTP). Bundled en la misma rama que Issue 7 (`fix/issue-7-credits-vacios`) por coincidir en sesion, pero es una causa raiz independiente (routing, no rate-limiting/credits). Verificacion pendiente: no se disparo una corrida real de `workflow_dispatch` contra produccion en esta sesion (accion con impacto en prod); queda para disparar manualmente tras el merge y confirmar `synced`>0 en los 4 tipos.
2026-08-13 | FE-20 library_backlog | Control de estado de biblioteca (`want`/`in_progress`/`completed`/`dropped`) en el detalle de item: `ViewerStatusSlot` (antes placeholder de FE-10) reescrito como Client Component que fetchea su propio estado tras montar via BFF `GET /api/{type}/{slug}/library` (mismo motivo que `RatingWidget`: el fetch publico/ISR del detalle nunca manda auth), con `PUT`/`DELETE` optimistas, guard `pending` anti doble-submit (mismo fix de FE-19) y rollback+toast en error. Nueva pagina publica `/u/{username}/library` (perfil completo aun no existe, FE-21 pending): tabs de status con counts de `GET /v1/users/{username}` (`library_counts`, zero-filled, sin pedir la lista 4 veces), tabs de type sin counts (backend no los desglosa), grid cross-tipo con `CatalogCard`+`typeLabel`, paginacion server-rendered (`library-pagination.tsx`, sibling de `browse-pagination.tsx`), estados vacios/error inline. Nuevo `lib/library.ts` (dispatch per-tipo PUT/DELETE + `getViewerLibraryStatus` reusando `GET /v1/{type}/{slug}` autenticado, sin endpoint dedicado "mi status") con `lib/library-types.ts` separado (vocabulario puro sin `server-only` transitivo, mismo split que `catalog-types.ts`/`catalog.ts`, necesario porque el Client Component importa el vocabulario). Sin `revalidateTag` en el BFF autenticado (a diferencia de rating): `viewer_status` es por-usuario y nunca aparece en el fetch cacheado publico, verificado leyendo `getItemDetail`. Desviacion documentada del plan: no se creo BFF publico `api/users/[username]/library` (paso 3) por no tener consumidor cliente -- la pagina llama directo a `getUserLibrary`/`getUserProfile`, mismo patron que `listCatalog`/`searchCatalog`; verificado por el reviewer (sin fetch client a esa ruta en ningun componente). i18n nuevo `Library.*` + `ItemDetail.library.*` en/es. Pipeline verde: typecheck/lint/432 tests frontend/build + `bash init.sh` (702 backend, sin tocar `backlogg/`/`tests/`). Reviewer: APPROVED (`progress/review_fe20.md`), sin cambios requeridos.
2026-08-14 | FE-24 notifications (id 25) | Campana con badge de `unread_count` + dropdown de notificaciones + marcar como leídas. Bloqueante detectado al empezar: `NotificationTargetOut` (`review_like`) solo exponía `target_id` = id numérico de `user_ratings.id`, sin forma de resolverlo a slug/tipo de item desde el frontend — decisión del usuario (entre 3 opciones presentadas): enriquecer el backend en vez de degradar el link al perfil de quien dio like. Fase 1 (backend, fuera de `feature_list.json`, mismo tratamiento que bugfix — reviewer en texto sin archivo): `backlogg/notifications/schemas.py` (`NotificationTargetOut` gana `item_type`/`slug`, null para `new_follower`) + `repository.py` (`list_notifications` resuelve el target en la misma query vía LEFT JOIN condicional a `user_ratings` + un LEFT JOIN por modelo de `ITEM_MODELS` + `COALESCE`, sin N+1, reusando el mapeo de `ratings/repository.py` en vez de duplicar como hace `feed/repository.py` con su UNION ALL) + `service.py` + `docs/api.md` + tests nuevos en `tests/notifications/` (4 tipos de item, `new_follower` con target null, review oculta con target igual resuelto porque el destino es el item no la review). Fase 2 (frontend, `progress/review_fe24.md`, `APPROVED`): `packages/api-client` regenerado (`gen:api`) para reflejar el schema nuevo; `lib/notifications-types.ts` (`notificationHref` centraliza linking: `new_follower`→`/u/{username}`, `review_like`→`/{item_type_lowercase}/{slug}`, sin link si no resuelve) + `lib/notifications.ts` (mismo esqueleto que `feed.ts`) + 3 rutas BFF nuevas bajo `api/notifications/*` (colapsan `ok:false` a 502 genérico, distinto de `rating`/`follow` que sí preservan 401/404 reales — justificado porque `NotificationBell` solo monta para sesión ya verificada server-side, anotado por el reviewer como criterio a revisar si aparece una superficie de error mayor) + `NotificationBell` (Client Component, dropdown Radix, badge oculto en 0 y cap "99+", marca-todas-como-leídas al abrir el dropdown en vez de por-item — sin UI de check por fila, decisión que el reviewer valida como no sobre-ingeniería) + entrada en `site-header.tsx` gateada por sesión (mismo criterio que `/feed`) + i18n `Notifications.*` en/es. Sin página dedicada `/notifications` (dropdown cubre el acceptance completo). Pipeline verde: typecheck/lint/544 tests frontend/build + `pnpm -r typecheck` + `bash init.sh` (718 backend). Pendiente: QA manual + confirmación del usuario para commit/push/PR desde `feat/fe25-notifications`.
2026-08-14 | refinamiento fe24_qa_fixes (fuera de backlog, feedback de QA manual del usuario sobre FE-24 ya done, misma rama `feat/fe25-notifications`) | 3 fixes en `NotificationBell` tras probar en navegador: (1) número del badge descentrado — causa confirmada visualmente con Playwright (login real + notificaciones reales via API, zoom 4x): faltaba `leading-none` en el `<span>` del badge, el line-height por defecto de `text-[10px]` desplazaba el glifo dentro del flex container pese a `items-center`/`justify-center`; (2) abrir el dropdown marcaba automáticamente todas las notificaciones como leídas sin acción del usuario — cambiado a acción explícita: nuevo `DropdownMenuItem` "mark all as read" (oculto si `unreadCount` es 0/null, `event.preventDefault()` para no cerrar el dropdown en el click) que dispara el `POST /api/notifications/read` existente; `handleOpenChange` ya solo hace `GET`, sin marcar nada; (3) notificaciones ya leídas no debían listarse — filtrado client-side (`!item.is_read`) sobre la respuesta del `GET`, sin tocar el backend (sin filtro `is_read` en `GET /v1/notifications`, fuera de scope). Tras "mark all as read", vaciado optimista de la lista + badge a 0 sin refetch (justificado: el endpoint marca todas las no-leídas del caller y la lista visible ya solo contenía no-leídas). Verificación visual con Playwright (`node_modules/.pnpm/playwright@1.62.1/...`, import directo por path — `playwright` no está hoisteado top-level en `apps/web/node_modules`) contra usuarios de prueba reales (`qabell_a_*`/`qabell_b_*`, notificaciones generadas via API real): confirmó badge centrado, botón visible solo con no-leídas, reabrir sin pulsar el botón no marca nada, click en el botón vacía la lista y resetea el badge. i18n: `Notifications.markAllRead` en/es. Tests actualizados en `notification-bell.test.tsx` (4 nuevos/reescritos) cubriendo los 3 puntos. Pipeline verde: typecheck/lint/546 tests frontend/build + `bash init.sh` (718 backend). Reviewer: APPROVED en texto (sin `progress/review_*.md`, refinamiento fuera de backlog), con un nit no bloqueante señalado (doc comment desactualizado en `api/notifications/read/route.ts`, queda pendiente para un commit de seguimiento). Pendiente: confirmación del usuario para commit/push/PR desde `feat/fe25-notifications`.
2026-08-14 | refinamiento fe24_qa_fixes_round2 (fuera de backlog, feedback de QA manual del usuario, misma rama `feat/fe25-notifications`) | 2 problemas más tras seguir probando en navegador. (1) Badge del bell: `text-destructive-foreground` no existía como variable CSS en `globals.css` (solo `--destructive`, sin foreground; ningún otro componente del repo la usaba) — texto del badge quedaba sin contraste real. Fix: color explícito por tema (`text-white dark:text-neutral-950`) en vez de un token de diseño global nuevo, justificado con cálculo de contraste WCAG (OKLCH→sRGB→luminancia) contra los dos valores reales de `--destructive`: blanco/claro ≈4.76:1, negro/oscuro ≈7.26:1 (blanco sobre el rojo oscuro daba solo ≈2.89:1, de ahí el override); verificado con el mismo cálculo independiente por el reviewer. (2) El corazón de "like" en reseñas (`item-reviews.tsx`, FE-19 ya done) siempre arrancaba en "no liked" sin importar el estado real (`RatingOut` no exponía si el caller ya había dado like), desincronizando el toggle del backend en sesiones sucesivas — root cause confirmado por curl con dos cuentas reales antes de tocar código; el pipeline de notificaciones de FE-24 en sí no tenía el bug. Decisión del usuario: bundlear el fix en esta misma rama en vez de una `fix/` separada, aunque el archivo vive en otra feature ya done (memoria nueva: `bundle-qa-blocking-bugs-same-branch`). Fix backend: `RatingOut.liked_by_viewer: bool` nuevo, resuelto sin N+1 en `ratings/repository.py` (`EXISTS` correlacionado igual patrón que `like_count_subq`, `literal(False)` directo si `caller_id is None`) + `caller_id` propagado por `ratings/service.py` + los 4 route handlers (movies/series/books/games) inyectando `get_current_user_optional` (endpoint sigue público) + `docs/api.md`. Gap adicional descubierto por el implementer verificando end-to-end en navegador (no solo curl): el BFF `api/[type]/[slug]/ratings/route.ts` no reenviaba el token del caller al backend en absoluto, así que `liked_by_viewer` habría llegado siempre `false` vía la app aunque el backend estuviera bien — arreglado con `apiFetch`+`authHeader` (mismo patrón que `rating/route.ts`) y `getCurrentUser()` secuencial antes de `listRatings` (evita carrera de doble-refresh de token, precedente ya existente en `rating/route.ts`). `item-reviews.tsx` usa `liked_by_viewer` como estado inicial real; doc comment "Known limitation" (documentaba exactamente este gap) actualizado. `packages/api-client` regenerado. Tests nuevos backend (`tests/ratings/`: liker/stranger/anónimo en las 3 capas) y frontend (`ratings.test.ts`, `route.test.ts`, `item-reviews.test.tsx`). Verificación visual end-to-end con Playwright (usuarios reales `qabell_a_*`/`qabell_b_*`): badge legible en ambos temas, corazón arranca liked en carga fresca sin toggle manual previo, ciclo real unlike→like desde la UI genera notificación visible para el autor. Pipeline verde: `bash init.sh` (725 backend) + typecheck/lint/550 tests frontend/build + `pnpm -r typecheck`. Reviewer: APPROVED en texto (sin `progress/review_*.md`, refinamiento fuera de backlog), sin cambios requeridos. Pendiente: confirmación del usuario para commit/push/PR desde `feat/fe25-notifications`.
2026-08-14 | refinamiento fe24_qa_fixes_round3 (fuera de backlog, petición del usuario tras QA de las rondas 1/2, misma rama `feat/fe25-notifications`) | Botón de eliminar por notificación individual (el botón "mark all as read" de rondas previas queda tal cual, confirmado por el usuario). Backend: nuevo `DELETE /v1/notifications/{id}` — `backlogg/notifications/repository.py::delete_notification` scoped por `recipient_id` en el `WHERE` (un id de otro usuario no borra nada), `service.py` solo hace `commit()` si se borró algo, `routes.py` 204/404 sin distinguir "no existe" de "es de otro" (mismo criterio que `DELETE /v1/{type}/{slug}/rating`), auth requerida. `docs/api.md` documentado. Tests en las 3 capas (propia/ajena/inexistente). Frontend: `lib/notifications.ts::deleteNotification` + nueva ruta BFF `api/notifications/[id]/route.ts` (mismo esqueleto que las rutas hermanas, validación de id igual que `api/reviews/[id]/like/route.ts`) + botón de eliminar (icono X) en `NotificationRow` — reestructurado el `<li>` para que el botón sea HERMANO del `Link`/`div` de contenido, no anidado dentro (evita interactivo-dentro-de-interactivo inválido en HTML y que el click dispare la navegación); `preventDefault`/`stopPropagation` en el click; estado local (`items`/`unreadCount`) solo se actualiza en éxito (204), fila permanece si falla. `packages/api-client` regenerado. i18n `Notifications.deleteAriaLabel` en/es. Reviewer primera pasada: `CHANGES REQUESTED` — único hallazgo, `bruno/Notifications/Delete notification.bru` aceptaba `[204, 404]` en el mismo test en vez de un assert determinista, violando el patrón ya establecido en el repo (`bruno/Ratings/Delete movie rating.bru` + `... — not found (404).bru`). Corregido: `bruno/Notifications/` pasó de 4 a 8 requests (3 nuevos de setup — registro/login/follow de un segundo usuario para generar una notificación real — más `List notifications.bru` capturando el id real vía `vars:post-response`, y los dos deletes con un único assert exacto cada uno, 204 y 404). Segunda pasada del reviewer: `APPROVED`, confirmando además que ningún archivo Python/TypeScript se tocó en el fix puntual del bruno. Pipeline verde: `bash init.sh` (734 backend) + typecheck/lint/562 tests frontend/build + `pnpm -r typecheck`. Pendiente: confirmación del usuario para commit/push/PR desde `feat/fe25-notifications`. Nota aparte (no implementada): el usuario preguntó si comentarios/conversaciones anidadas sobre reviews estaban contempladas en el backlog — confirmado que no, ni en `feature_list.json` ni `frontend_feature_list.json` ni `docs/`; queda pendiente de decidir si se loguea como nueva feature futura.
2026-08-15 | FE-25 lists_crud (id=26, frontend_feature_list.json) | CRUD de listas curadas en apps/web: crear (create-list-dialog.tsx, POST /v1/lists via BFF app/api/lists/route.ts), editar título/descripción/is_public (edit-list-dialog.tsx, PATCH) y borrar con confirmación (delete-list-dialog.tsx, DELETE), ambos via app/api/lists/[slug]/route.ts (apiFetch + auto-refresh). Listado en perfil (user-list-card.tsx reemplaza el ProfileListCard inline) via GET /v1/users/{username}/lists; user-content.ts.getUserLists ahora reenvía el Authorization del viewer para que el owner vea sus propias listas privadas (bug real de FE-21 cerrado de paso). Validación cliente con zod (lib/lists/schemas.ts) espejando ListCreate/ListUpdate; feedback 401/403/404/422 diferenciado con i18n propio (Profile.lists.*, en/es en sync) y toasts sonner + router.refresh(). Fuera de alcance respetado: sin página de detalle de lista ni gestión/reorder de items (FE-26 list_items, depends_on=[26], sigue pending). Tests junto a cada archivo nuevo (happy path + 401/403/404/422/500 + validación cliente). Reviewer: APPROVED — typecheck/lint/vitest(617)/build re-ejecutados de forma independiente, todo en verde. QA manual pendiente de confirmación del usuario antes de shippear.

2026-08-16 | games_similar_igdb (id=45, feature_list.json) | GET /games/{slug}/similar, mismo contrato que /movies/{slug}/similar y /series/{slug}/similar (feature 16): backlogg/games/adapters/igdb.py ahora pide similar_games.* en la query POST /games de get_game_by_slug; backlogg/games/service.py::get_similar_games sigue el mismo patron de 4 pasos que movies/series (lookup local -> 404, external_id -> results vacio si no hay, fetch de similares via IGDB, upsert automatico de los no presentes en DB local con conversion explicita de first_release_date a date antes de persistir), capado a 10 resultados; backlogg/games/schemas.py anade SimilarGameOut/SimilarGameListOut; backlogg/games/routes.py expone la ruta nueva sin logica de negocio. docs/api.md y docs/external-apis.md actualizados. Tests de servicio con IGDBClient mockeado incluyendo el caso sin similares (clave ausente o vacia) -> results vacio sin excepcion, mas tests de router e igdb client. Coleccion Bruno anadida (happy path + 404). Reviewer: APPROVED sin cambios requeridos (bash init.sh: 702 tests en verde, ruff check/format en verde). QA manual pendiente de confirmacion del usuario antes de shippear.
2026-08-16 | FE-28 (id 27) admin_auth | apps/web/src/lib/env.ts (`env.ADMIN_API_KEY`, mismo patrón lazy/`server-only` que `API_INTERNAL_URL`) + apps/web/.env.example (placeholder) + apps/web/src/app/api/admin/stats/route.ts (Route Handler `GET /api/admin/stats`, único punto que lee la key e inyecta `X-API-Key` hacia `GET /v1/admin/stats`; distingue 4 casos con status propios: key del BFF ausente → 503 `not_configured` sin llamar al backend, backend 401 → 401 `unauthorized`, backend 503 → 503 `not_configured`, otro fallo → 502 `admin_stats_failed`) + apps/web/src/components/admin-stats-panel.tsx (Client Component que consume el Route Handler, nunca ve la key, traduce los 3 status a mensajes i18n distintos) + apps/web/src/app/[locale]/admin/page.tsx (Server Component, mismo patrón que `/settings`/`/recommendations`: `generateMetadata` con `robots: noindex`, `getCurrentUser()`+redirect a `/login`) + apps/web/src/lib/auth/protected-routes.ts (`/admin` añadido a `PROTECTED_PREFIXES`, doble capa junto al gate del Server Component) + i18n `Admin.*`/`Metadata.admin` en en/es. Limitación documentada explícitamente: es un gate de "sesión iniciada", no autorización admin real — el backend no tiene rol admin, `/v1/admin/*` está protegido enteramente por el secreto `X-API-Key`, que ni esta página ve nunca; fiel al scope de FE-28 (FE-29 cola de reportes y FE-30 moderación quedan para features futuras, no implementadas aquí). Tests nuevos: `route.test.ts` (5 casos, incluye spy que confirma que no se llama al backend si falta la key propia, y assert de que la key nunca aparece en el body de respuesta) + `admin-stats-panel.test.tsx` (5 casos: loading, éxito con `last_synced_at` null, 401, 503, error genérico). Sin `page.test.tsx` (sin precedente para páginas Server Component protegidas del repo). Pipeline verde: typecheck/lint/build + 583 tests (73 archivos). Reviewer: APPROVED sin cambios requeridos — verificación propia independiente del pipeline completo, grep de `ADMIN_API_KEY` sobre el bundle cliente compilado (0 coincidencias) y de scope creep hacia FE-29/FE-30 (0 coincidencias). QA manual pendiente de confirmación del usuario antes de shippear.
2026-08-16 | bugfix admin_role_gate (fuera de backlog, feedback de QA manual del usuario sobre FE-28 ya done, misma rama `feat/admin_auth`) | Durante el QA de FE-28 el usuario señaló correctamente que el gate de `/admin` solo comprobaba "¿hay sesión?", no "¿es admin?" — el backend no tenía ningún concepto de rol admin (`users` solo tenía `is_banned`), así que en producción cualquier usuario autenticado podía cargar `/admin` y el BFF le inyectaba la `ADMIN_API_KEY` compartida sin comprobar quién era. El usuario pidió explícitamente la solución a largo plazo (rol real en DB) en vez de un allowlist de usernames por variable de entorno. Backend: `backlogg/users/models.py` (`User.is_admin: bool`, mismo patrón que `is_banned` — `Boolean`, `nullable=False`, `server_default="false"`) + `alembic/versions/0020_user_is_admin.py` (`down_revision="0019"`, `ALTER TABLE users ADD COLUMN is_admin BOOLEAN NOT NULL DEFAULT false`, downgrade reversible) + `backlogg/users/schemas.py` (`is_admin` añadido solo a `UserMeOut`, perfil propio — **no** a `UserOut` público, evita fuga del rol de otros usuarios) + `docs/schema.md`/`docs/api.md` actualizados. Deliberadamente sin ningún endpoint API para leer/activar `is_admin` — se activa a mano en la DB por un operador, para no crear una superficie de escalado de privilegios. Tests nuevos en `tests/users/` (default `False` en usuario nuevo + presencia/valor en `GET /v1/users/me`). Reviewer backend: APPROVED en texto (sin `progress/review_*.md`, fix fuera de backlog), verificación propia de la cadena de migraciones (`alembic heads`/`history`, sin ramas divergentes), de que `is_admin` no aparece en ningún `routes.py` nuevo, y de `bash init.sh` (708 tests). Frontend: `packages/api-client` regenerado (`is_admin` en `UserMeOut`) + `apps/web/src/app/[locale]/admin/page.tsx` (segundo check tras el gate de sesión: `!user.is_admin` → `redirect` a `/`, no a `/login`, porque el usuario ya está autenticado, solo no autorizado; negación truthy, no `=== false`, así que `undefined`/`null` también redirige) + doc-comment de `apps/web/src/app/api/admin/stats/route.ts` actualizado (ya no describe el gate de sesión como límite final, ahora documenta que la autorización real por rol vive en `admin/page.tsx`, su único caller). Nuevo `admin/page.test.tsx` (3 casos: sin sesión→`/login`, con sesión sin admin→`/` explícitamente no `/login`, con sesión admin→renderiza) — excepción deliberada al "sin precedente de test de página" de FE-28, justificada por ser exactamente la lógica de autorización que este fix introduce. Reviewer frontend: APPROVED en texto, verificación propia e independiente del pipeline completo (typecheck/lint/586 tests/build) y confirmación de que ningún camino con sesión pero `is_admin` no estrictamente `true` llega a renderizar el panel admin. Sin página 403 dedicada (sin precedente en el repo, fuera del scope acotado de este fix). Pendiente: el usuario necesita activar `is_admin=true` a mano en su propio usuario de la DB de dev para completar el QA manual, y confirmación explícita antes de commit/push/PR (bundleado en el mismo commit/PR que FE-28, misma rama).
