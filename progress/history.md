# Historial de sesiones

<!-- Append-only. Añadir una línea al cerrar cada feature. -->
<!-- Formato: YYYY-MM-DD | feat_<id> <name> | resumen de una línea -->
2026-08-08 | feat_40 response_caching | backlogg/core/cache.py (interfaz Cache ABC + InMemoryTTLCache con TTL por entrada/reloj inyectable/expiración perezosa + singleton get_cache()) + backlogg/core/http_cache.py (CacheHeadersMiddleware: clasifica por route template y aplica Cache-Control + ETag/304 en detalle; detalle anónimo public+max-age+ETag+Vary:Authorization, detalle autenticado private,no-cache, feed//users/me private,no-store) + config.py (CACHE_CONTROL_DETAIL_MAX_AGE=300, CACHE_CONTROL_LISTING_MAX_AGE=60, CACHE_TTL_TRENDING=900, CACHE_TTL_GENRES=300) + main.py (middleware más interno) + genres/service.py y trending/service.py (consultan get_cache() antes del repo) + conftest.py (fixture autouse _reset_response_cache) + .env.example + tests/core/test_cache.py + tests/test_response_caching.py (+18 tests). 632 tests en verde. Reviewer: APPROVED.
2026-08-08 | feat_38 observability | backlogg/core/observability.py (JSONFormatter stdlib, redacción de secretos por campos extra + texto libre/tracebacks incl. dict-repr/JSON/Bearer, configure_logging idempotente, ContextVar + RequestIDMiddleware con X-Request-ID entrante/generado + log request.completed método/path/status/duración, init_sentry perezoso gated por SENTRY_DSN) + config.py (LOG_LEVEL, SENTRY_DSN) + main.py (wiring + @app.exception_handler(Exception) 500 correlacionado) + pyproject.toml/uv.lock (sentry-sdk[fastapi]) + .env.example + docs/operations.md + tests/test_observability.py. 599 tests en verde. Reviewer: APPROVED (2ª ronda tras reforzar redacción AC3).
2026-06-21 | feat_22 cors_and_security | CORSMiddleware (CORS_ORIGINS env var, default localhost:3000+5173) + SecurityHeadersMiddleware (X-Content-Type-Options, X-Frame-Options, Referrer-Policy) en main.py + 8 tests. 185 tests en verde.
2026-06-21 | feat_21 credits_in_detail | shared/schemas.py (CreditOut) + shared/credits.py (get_credits_for_item JOIN credits+people ORDER BY billing_order ASC NULLS LAST) + MovieOut/SeriesOut/GameOut extendidos con credits[] + servicios actualizados para construir Pydantic explícito con credits inyectados + 6 tests nuevos (2 por dominio: empty y ordered). 164 tests en verde.
2026-06-21 | feat_19 book_authors_people | get_author() en OL adapter + _persist_book_authors() en service + sync_books() actualizado + _resolve_credits() extendido a BOOK + 8 tests nuevos. 158 tests en verde.
2026-05-24 | feat_1 shared_models | pyproject.toml + core (config, database) + shared/models.py (Person, Credit) + shared/external_ids.py (ExternalId, helpers upsert/get/set) + migración Alembic 0001 (external_ids, people, credits con triggers) + 5 tests en verde.
2026-05-24 | feat_2 movies | movies/models.py (Movie, MovieGenre, join) + movies/schemas.py (MovieOut, GenreOut) + movies/repository.py (get_by_slug, upsert con get-or-create genres) + movies/adapters/tmdb.py (TMDBClient: search + detail + slugify) + movies/service.py (on-demand fallback) + movies/routes.py (GET /movies/{slug}) + main.py (FastAPI app + /health) + migración Alembic 0002 + 14 tests en verde (4 repo + 3 servicio + 2 rutas + 5 heredados).
2026-05-24 | feat_3 series | series/models.py (Series, SeriesGenre, join) + series/schemas.py (SeriesOut, SeriesGenreOut) + series/repository.py (get_by_slug, upsert con get-or-create genres) + series/adapters/tmdb.py (TMDBSeriesClient: search_series /search/tv + get_series_detail /tv/{id} + series_to_dict) + series/service.py (on-demand fallback) + series/routes.py (GET /series/{slug}) + main.py actualizado + migración Alembic 0003 (series_genres, series, series_genres_join, trigger) + 8 tests nuevos, 22 en total. Reviewer: APPROVED, todos los checkpoints C1-C17, C20-C22 superados.
2026-05-24 | feat_4 books | books/models.py (Book, BookGenre, join) + books/schemas.py (BookOut, BookGenreOut) + books/repository.py (get_by_slug, upsert con get-or-create genres) + books/adapters/open_library.py (OpenLibraryClient: search_book /search.json + get_work_detail /works/{olid}.json + book_to_dict + _parse_ol_date) + books/service.py (on-demand fallback) + books/routes.py (GET /books/{slug}) + main.py actualizado + migración Alembic 0004 (book_genres, books, book_genres_join, trigger set_updated_at_books) + 9 tests nuevos (4 repo + 3 servicio + 2 router), 31 en total. Reviewer: APPROVED, todos los checkpoints C1-C17, C20-C22 superados. Open Library no provee rating (None). Géneros derivados de subjects (primeros 10). Slug = slugify(title)-year.
2026-05-24 | feat_6 people | backlogg/people/ (models.py, schemas.py, repository.py, service.py, routes.py) + movies/service.py (_persist_movie_people: ACTOR top10 + DIRECTOR) + series/service.py (_persist_series_people + _persist_series_creators: ACTOR top10 + CREATOR) + movies/adapters/tmdb.py (get_movie_credits) + series/adapters/tmdb.py (get_series_credits) + upsert idempotente en people/external_ids/credits + GET /people/{slug} (200 con créditos resueltos, 404 si no existe) + main.py actualizado + 11 tests nuevos (3 repo + 3 servicio + 5 router), 58 en total. Reviewer: APPROVED, todos los checkpoints superados. Sin migración nueva (people/credits ya existían en 0001).
2026-06-20 | feat_17 search_external_fallback | search/service.py fan-out paralelo (asyncio.gather) a TMDB/OpenLibrary/IGDB cuando 0 resultados locales; games/adapters/igdb.py añade search_games(); refresh_catalog_search movido a repository.py; 8 tests nuevos (139 total). Reviewer: APPROVED.
2026-05-24 | feat_5 games | games/models.py (Game, GameGenre, GamePlatform, Company, CompanyCredit, joins) + games/schemas.py (GameOut, GameGenreOut, GamePlatformOut) + games/repository.py (get_by_slug, upsert_game con genres/platforms/companies) + games/adapters/igdb.py (IGDBClient: OAuth2 Twitch client_credentials, token renewal con buffer 60s, get_game_by_slug, get_top_games, game_to_dict) + games/service.py (on-demand fallback) + games/routes.py (GET /games/{slug}) + main.py actualizado + migración Alembic 0005 (game_genres, games, game_genres_join, game_platforms, game_platforms_join, companies, company_credits, triggers) + 16 tests nuevos (4 repo + 3 servicio + 2 router + 7 cliente IGDB), 47 en total. Reviewer: APPROVED, todos los checkpoints superados. IGDB rating 0-100 normalizado a 0-10. game_type mapeado de int a string. first_release_date Unix timestamp → date.
2026-05-24 | feat_7 catalog_search | alembic/versions/0006_catalog_search.py (vista materializada UNION ALL de movies+series+books+games con tsvector + índice GIN) + backlogg/search/__init__.py + search/models.py (CatalogSearchEntry ORM read-only, PK compuesta slug+item_type) + search/repository.py (plainto_tsquery + ts_rank para ranking, filtro por item_type, paginación) + search/service.py + search/schemas.py (SearchResult, SearchResponse) + search/routes.py (GET /search, q requerido min_length=1 → 422, filtro type, paginación page/limit) + main.py actualizado + tests/test_search.py (8 tests: happy path, filtro tipo, exclusión, paginación, 422 sin q, 422 q vacío, sin resultados, campos esperados; fixture con REFRESH MATERIALIZED VIEW). 66 tests en total. Reviewer: APPROVED, todos los checkpoints C1-C22 aplicables superados.
2026-05-24 | feat_8 scheduler_sync | scheduler/__init__.py + scheduler/jobs.py (sync_movies, sync_series, sync_books, sync_games, cada uno con aislamiento de errores y REFRESH MATERIALIZED VIEW CONCURRENTLY al final) + scheduler/setup.py (AsyncIOScheduler con 4 CronTrigger: 02:00, 02:30, 03:00, 03:30 UTC) + admin/__init__.py + admin/schemas.py (SyncResponse Pydantic v2) + admin/router.py (POST /admin/sync/{type}, 202, response_model=SyncResponse, fire-and-forget via asyncio.create_task) + main.py (lifespan con scheduler start/stop, admin router registrado) + adapters: get_top_movies (TMDB /movie/popular), get_top_series (TMDB /tv/popular), get_trending_books (Open Library /trending/weekly) + tests/test_admin_sync.py (12 tests). 78 tests en total. Reviewer: APPROVED tras corrección de C10 (response_model Pydantic v2).
2026-05-25 | feat_10 nightly_sync_trigger | .github/workflows/nightly-sync.yml — cron 0 2 * * * + workflow_dispatch, llama a POST /admin/sync/{movie,series,book,game} usando secret RENDER_API_URL. Solución para Render free tier (servicio se apaga por inactividad). 78 tests en verde.
2026-05-24 | feat_9 deployment | Dockerfile (multi-stage: uv builder + python:3.12-slim runtime, usuario no-root, puerto 8080) + entrypoint.sh (alembic upgrade head → exec uvicorn) + .dockerignore + fly.toml (app=backlogg-api, región mad, 512MB shared CPU, auto-stop, force_https) + .github/workflows/ci.yml (push/PR a main, PostgreSQL 16 service, uv, bash init.sh). Sin cambios en backlogg/ ni tests/. 78 tests en verde. Reviewer: APPROVED.
2026-05-25 | feat_11 admin_stats | admin/repository.py (get_stats: 4 queries COUNT+MAX(last_synced_at)) + admin/service.py (get_stats → StatsResponse) + admin/schemas.py (ContentStats, StatsResponse Pydantic v2) + admin/router.py (GET /admin/stats delegando al servicio) + tests/test_admin_stats.py (6 tests). 84 tests en verde. Reviewer: APPROVED tras corrección de separación de capas (router sin imports SQLAlchemy).
2026-05-26 | feat_11 admin_stats (ext) | sync síncrono: scheduler/jobs.py retorna {"synced", "errors", "duration_s"} + admin/schemas.py SyncResponse actualizado (sin status, con synced/errors/duration_s) + admin/router.py (await handler, status_code=200, sin asyncio) + tests/test_admin_sync.py (patch.dict sobre _SYNC_HANDLERS) + nightly-sync.yml (--max-time 600, sin sleep). 84 tests en verde. Reviewer: APPROVED.
2026-05-26 | feat_13 catalog_search_unique_index | alembic/versions/0007_catalog_search_unique_index.py — CREATE UNIQUE INDEX uq_catalog_search_type_id ON catalog_search (item_type, id) para habilitar REFRESH MATERIALIZED VIEW CONCURRENTLY. downgrade con DROP INDEX IF EXISTS. 87 tests en verde. Reviewer: APPROVED.
2026-06-20 | feat_14 list_endpoints | movies/series/books/games: añadidos *SortEnum, *ListItemOut, *ListOut en schemas; list_* en repository (join géneros, filtro por slug, ORDER BY enum, COUNT total, offset/limit) y service (proyección ORM→Pydantic, mapeo first_air_date/first_publish_date→release_date); GET "" en cada router. 4 test files (24 tests nuevos), 111 tests en verde. Sin migración. Reviewer: APPROVED.
2026-06-20 | feat_15 genres_endpoint | backlogg/genres/ (módulo nuevo: __init__.py, schemas.py, repository.py con UNION ALL de 4 subqueries, service.py, routes.py) + main.py actualizado (genres_router registrado) + tests/test_genres.py (10 tests: all types, filtro movie/series/book/game, 422 invalid, campos response, counts reales). 121 tests en verde. Reviewer: APPROVED.
2026-06-20 | feat_16 similar_items | movies/adapters/tmdb.py + series/adapters/tmdb.py (get_*_recommendations via TMDB /recommendations) + schemas SimilarMovieOut/SimilarMoviesOut + SimilarSeriesOut/SimilarSeriesListOut + service get_similar_movies/get_similar_series (lookup external_id TMDB, fetch recomendaciones, persiste nuevos, devuelve top-10) + routes GET /movies/{slug}/similar y GET /series/{slug}/similar (declarados ANTES de /{slug}) + 10 tests de servicio. 131 tests en verde. Reviewer: APPROVED.
2026-06-21 | feat_20 trending_popular | trending/__init__.py + schemas.py (ItemTypeEnum, PeriodEnum, TrendingItemOut, TrendingOut) + service.py (TMDB trending fan-out paralelo para movie+tv, persist via upsert_movie/upsert_series, top 20) + router.py (GET /trending con type y period) + movies/adapters/tmdb.py get_trending_movies + series/adapters/tmdb.py get_trending_series + main.py actualizado + 11 tests nuevos. 169 tests en verde. Reviewer: APPROVED.
2026-07-04 | fix nightly-sync-workflow (PR #39) | Las runs nocturnas fallaban 10+ días: 4 jobs paralelos saturaban la instancia free de Render (books → error HTTP, movies → timeout 600s). Workflow reescrito: 1 job secuencial + wake-up /health + max-time 1800 + retries idempotentes. Run manual: success 28m14s.
2026-07-05 | refactor remove-inprocess-scheduler (PR #40) | Eliminado APScheduler in-process (código muerto: la instancia free duerme y los CronTrigger nunca disparaban; el sync real es GitHub Actions). scheduler/jobs.py conservado (lo usan /admin/sync/{type}). Paso verify del workflow endurecido: falla si algún last_synced_at > 2h. Docs actualizadas. Reviewer: APPROVED.
2026-07-05 | fix test-db-isolation (PR #41) | tests/conftest.py reescrito con guardia de aislamiento (aborta si TEST_DATABASE_URL falta, coincide con DATABASE_URL o database se importó antes), redirección global a backlogg_test + TRUNCATE inicial. tests/test_conftest_guard.py (6 tests). 218 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_23 seed_config_wiring | scheduler/jobs.py: 4 limit=100 hardcodeados sustituidos por settings.SEED_TOP_N_* (lectura en runtime) + tests/test_sync_seed_limits.py (4 tests con monkeypatch). 222 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_24 sync_slice_cursor | Nueva tabla sync_cursors (migración 0008_sync_slice_cursor) + backlogg/scheduler/repository.py (get/set offset, upsert). Adapters con offset retrocompatible (TMDB→páginas de 20 con tope page 500, Open Library/IGDB offset nativo). jobs.py: sync por tramos de SYNC_SLICE_SIZE (default 200) con wraparound a 0 al agotar SEED_TOP_N_* o API corta; offset expuesto en POST /admin/sync/{type}. Docs (schema/api/external-apis) y 17 tests nuevos (cursor, adapters, wraparound, idempotencia). 239 tests en verde. Reviewer: APPROVED (1 ronda de cambios — rename de migración al patrón <revision>_<feature_name>).
2026-07-05 | feat_25 books_popular_source | open_library.py: get_trending_books → get_popular_books (GET /search.json?q=*:*&sort=readinglog con offset/limit nativos, fields=key,title,author_name,first_publish_year,cover_i,subject,isbn, paginación per_page=100) sustituyendo /trending/weekly.json (fuente capada a cientos de entradas); jobs.py sync_books actualizado; docs/external-apis.md con nota "Popular-books strategy"; tests renombrados + 1 nuevo de endpoint/params. 240 tests en verde. Reviewer: APPROVED.
2026-07-05 | feat_26 direct_backfill_sync | scripts/backfill_sync.py (CLI uv run, reutiliza sync_* de jobs.py vía getattr, bucle hasta wraparound del cursor o presupuesto de tiempo default 300min, aborta si synced=0+errors>0) + param opcional slice_size en los 4 jobs (None → settings.SYNC_SLICE_SIZE, capped por SEED_TOP_N_*-offset) + igdb.py get_top_games pagina >500 en bucle con throttle 0.3s (~3.3 req/s) + .github/workflows/backfill-sync.yml (workflow_dispatch: content_type choice + seed_top_n default 10000, secrets DATABASE_URL/TMDB_API_KEY/TWITCH_CLIENT_ID/TWITCH_CLIENT_SECRET, timeout 350min) + docs/verification.md sección "Backfill manual" + tests/test_backfill_sync.py (14 tests mockeados). 254 tests en verde. Reviewer: APPROVED.
2026-07-06 | fix sync-genre-slug-collision | Backfill de books corría "success" con 0 persistidos: colisión uq_book_genre_slug (subjects distintos → mismo slug) envenenaba la sesión (InFailedSQLTransactionError en cascada) y el cursor no avanzaba. Fix: lookup slug-first en _get_or_create_genre de books/games (+platform; movies/series ya lo hacían) + dedupe intra-libro por slug + aislamiento por item en los 4 jobs (commit por item + _rollback_quietly con expunge_all). 7 tests nuevos (IntegrityError real). 261 tests en verde. Reviewer: APPROVED.
2026-07-06 | fix ol-error-masking | Un 500 transitorio de Open Library se enmascaraba como [] → sync_books lo leía como fin de resultados → cursor wrappeado a 0 y backfill "success" con 0 libros (run 28799265814). Fix: _fetch_popular_page con retry 5xx (3 intentos, backoff 1s/2s), raise en 4xx sin retry y en 5xx persistente; error a mitad de paginación descarta y lanza (cursor nunca wrappea por error). TMDB/IGDB verificados sin el patrón (ya usan raise_for_status). jobs.py y script sin cambios (sus guards ya hacían lo correcto al lanzar el adapter). 6 tests nuevos + 1 renombrado. 266 tests en verde. Reviewer: APPROVED.
2026-07-07 | docs docs/update-project-docs (PR #49) | README reescrito (quickstart, stack Render+GitHub Actions), docs/operations.md nuevo (runbook: nightly, backfill, admin endpoints, secrets, cursores), docs/api.md/architecture.md/verification.md actualizados al estado real. Merged.
2026-08-06 | backfill-catalog-complete | Confirmado via GET /admin/stats: movies=9371, series=9890, books=9976, games=10017, los 4 con last_synced_at=2026-08-06 (nightly diario). feat_26 (direct_backfill_sync) completó los 4 tipos: books ya estaba en 9974 desde el run 28827435184 (2026-07-06); movies/series/games se completaron en los dos runs del 2026-07-08 (28927353560 3h36m, 28927355351 1h47m) que no habían quedado documentados. Nightly sync corre sin fallos desde entonces (verificado hasta 2026-08-06). Backfill del catálogo: cerrado.
2026-08-06 | feat_27 users_auth | Arranca el épico social (auth + ratings/reviews + follows + feed; feature_list.json ampliado 27-30, CLAUDE.md/architecture.md/api.md actualizados — solo mensajería directa queda fuera de scope). backlogg/users/ (models, schemas, repository, service, auth con JWT/argon2, routes) + migración 0009_users.py + settings JWT_SECRET_KEY/JWT_ALGORITHM/JWT_EXPIRE_MINUTES + pyproject.toml (argon2-cffi, pyjwt) + main.py actualizado. Endpoints: POST /auth/register, POST /auth/login, GET/PATCH /users/me, GET /users/{username} (perfil público sin email). 39 tests nuevos, 305 en total. Reviewer: APPROVED (22 checkpoints, 1 observación no bloqueante: create_access_token no valida JWT_SECRET_KEY vacío — pendiente de configurar en Render antes de desplegar, igual que ADMIN_API_KEY).
2026-08-06 | feat_28 ratings_reviews | backlogg/ratings/ (models: UserRating + ReviewLike con UniqueConstraint polimórfico item_type/item_id estilo credits/external_ids; schemas; repository con upsert ON CONFLICT, recalculate_item_aggregates, like idempotente ON CONFLICT DO NOTHING; service; routes) + migración 0010_ratings_reviews.py (down_revision 0009) + endpoints añadidos a movies/series/books/games routes.py (PUT/DELETE /{slug}/rating, GET /{slug}/ratings con like_count) + POST/DELETE /ratings/{id}/like + GET /users/{username}/reviews (UNION ALL cross-type) + docs/schema.md y docs/api.md actualizados. Recalcula rating_internal (AVG)/rating_count_internal (COUNT) en el item afectado tras cada create/update/delete. 52 tests nuevos, 357 en total. Reviewer: APPROVED, 22 checkpoints + 12 criterios de acceptance verificados contra código (incluye C11: POST/DELETE /ratings/{id}/like usa id numérico en vez de slug, excepción deliberada documentada — una review no tiene slug natural). Ampliación post-QA manual: GET /{tipo}/{slug} no exponía rating_internal/rating_count_internal (se persistía pero no era visible vía API) — añadidos a MovieOut/SeriesOut/BookOut/GameOut y propagados desde el ORM en los 4 service.py; docs/api.md actualizado. Reviewer rechazó la primera vuelta (tests solo cubrían el caso None/0, sin probar la propagación real de un valor puntuado); corregido con 4 tests PUT rating + GET detail (uno por dominio) verificando el valor exacto. 361 tests en total. Reviewer: APPROVED en tercera ronda.
2026-08-07 | feat_29 follows | backlogg/follows/ (módulo nuevo: models Follow con UniqueConstraint(follower_id, followed_id)=uq_follow_pair, FKs a users ON DELETE CASCADE, índices idx_follows_follower/followed; schemas FollowUserOut/FollowListOut; repository con create_follow_if_not_exists (INSERT ON CONFLICT DO NOTHING → idempotente), delete_follow_if_exists (no-op tolerante), count_followers/following, list_followers/following paginados; service con self-follow 422 y target inexistente 404; routes bajo /users) + migración 0011_follows.py (down_revision 0010) + backlogg/users/schemas.py (UserOut extendido con follower_count/following_count) + backlogg/users/service.py (get_user_profile calcula counts vía follows.repository) + main.py (follows_router registrado) + docs/schema.md y docs/api.md actualizados. Endpoints: POST/DELETE /users/{username}/follow (auth, 204, idempotentes), GET /users/{username}/followers y /following (públicos, paginados). 32 tests nuevos (repo/service/routes: self-follow 422, doble follow idempotente, unfollow tolerante, 401/404, paginación, counts en perfil), 393 en total. Reviewer: APPROVED (22 checkpoints).
2026-08-08 | feat_30 activity_feed | Nuevo dominio backlogg/feed/ (repository.py único con SQLAlchemy, service.py, schemas.py FeedAuthorOut/FeedItemOut/FeedEntryOut/FeedListOut, routes.py feed_router prefix /feed) + GET /feed?tab=following|popular&page=&limit= (auth requerida, 401 sin token, 422 tab inválido) + UNION ALL cross-type sobre ITEM_MODELS con JOIN a users (autor sin N+1) y LEFT JOIN a subquery agregada de ReviewLike + coalesce(0) para like_count; following filtra por subquery embebida a follows (caller sin follows → vacío), popular ventana 30d ordenada por like_count desc/created_at desc; sin tablas ni migraciones nuevas + main.py registra feed_router + docs/api.md documentado + tests/feed/ (repo + endpoint). 405 tests en verde. Reviewer: APPROVED.
2026-08-08 | feat_31 user_library | Nuevo dominio backlogg/library/ (models LibraryEntry con status VARCHAR(20)+CHECK [want/in_progress/completed/dropped] y UniqueConstraint(user_id,item_type,item_id)=uq_library_entry_item, patrón polimórfico estilo ratings/credits; schemas LibraryStatus/LibraryTypeFilter/LibraryEntryIn/LibraryStatusOut/LibraryItemOut/LibraryEntryOut/LibraryListOut/LibraryCounts; repository con upsert ON CONFLICT DO UPDATE, get/delete, listado cross-type UNION ALL estilo feed/genres con release_date por tipo (movie→release_date, series→first_air_date, book→first_publish_date, game→release_date), counts zero-filled; service; routes) + migración 0012_user_library.py (down_revision 0011, reusa trigger_set_updated_at) + PUT/DELETE /{type}/{slug}/library en los 4 tipos (auth, 422/404) + GET /users/{username}/library (público, paginado, filtros ?status=/?type=) + viewer_status en los 4 GET detalle (nuevo get_current_user_optional en users/auth.py, auth opcional que nunca lanza 401; refactor books.get_book para construir BookOut explícito) + library_counts en GET /users/{username} (UserOut) + docs/schema.md y docs/api.md + bruno/Library/ (11 requests) + tests/library/ (40 tests). 445 tests en verde. Reviewer: APPROVED (22 checkpoints + acceptance verificadas). QA manual confirmado por el usuario.
2026-08-08 | feat_32 user_lists | Nuevo dominio backlogg/lists/ (models UserList [id, user_id FK→users CASCADE, slug, title, description?, is_public bool default true, timestamps; UniqueConstraint(user_id,slug)=uq_user_list_slug] y ListItem [list_id FK→user_lists CASCADE, item_type, item_id, position, added_at; UniqueConstraint(list_id,item_type,item_id)=uq_list_item]; schemas ListItemType/ListCreate/ListUpdate/ListItemRef/ListReorder/ListItemOut/UserListOut/UserListSummary/UserListsOut; repository única frontera SQLAlchemy reusando ITEM_MODELS/get_item_by_slug de ratings, resolución cross-type UNION ALL ordenada por position estilo library; service; routes lists_router /lists + user_lists_router /users/{username}/lists) + migración 0013_user_lists.py (down_revision 0012, reusa trigger_set_updated_at). Endpoints: POST /lists (201, slug derivado), PATCH/DELETE /lists/{slug} (owner 403/404), POST/DELETE /lists/{slug}/items ({item_type,slug}, idempotente, append + re-empaquetado de posiciones), PUT /lists/{slug}/items/order (valida set exacto → 422), GET /lists/{slug} (pública auth-opcional, privada 404 no-owner, items resueltos en orden), GET /users/{username}/lists (públicas siempre, privadas solo owner vía get_current_user_optional). Decisiones: colisión de slug con unicidad GLOBAL (sufijo -2/-3…, superconjunto de la constraint per-user, permite que /lists/{slug} resuelva a una única lista y distinga 404/403); slug no cambia en PATCH; payloads de items/order por {item_type,slug}; bug max(position) or -1 (0 falsy) corregido a check explícito de None. docs/schema.md y docs/api.md actualizados. 43 tests nuevos (repo/service/routes), 488 en verde. Reviewer: APPROVED (22 checkpoints + 12 criterios de acceptance). QA manual pendiente de confirmación del usuario.
2026-08-08 | feat_33 notifications | Nuevo dominio backlogg/notifications/ (models Notification con type enum [new_follower, review_like] + target polimórfico + índice (recipient_id, created_at desc), repository/schemas/service/routes) + migración 0014_notifications + wiring de generación en follows/service (new_follower solo en follow nuevo, no self) y ratings/service (review_like solo en like nuevo, autor != liker) vía RETURNING id para detectar inserción real + degradación elegante (commit del origen primero, helper try/except+rollback) + GET /notifications, GET /notifications/unread_count, POST /notifications/read + docs/schema.md y docs/api.md. Además fix del harness de tests preexistente: fixture db en tests/conftest.py migrado a patrón "transacción externa + SAVEPOINT" (join_transaction_mode=create_savepoint) que elimina la flakiness (~1/6) al hacer reversibles los commits de los handlers. 511 tests en verde (15/15 corridas). Reviewer: APPROVED.
2026-08-08 | feat_34 personalized_recommendations | Nuevo dominio solo-lectura backlogg/recommendations/ (schemas, repository, service, routes) + GET /recommendations (auth, ?type= con 422, page/limit). Semillas: ratings score>=4 + library completed/want (weight+source, dedup por (type,id)). Candidatos: solapamiento de género local (todos los tipos) + fan-out externo a similar de movies/series (feature 16) SOLO si faltan locales (gating verificado con assert_not_called). Exclusión de vistos (rated+library), reason legible, fallback populares por rating_external cuando no hay semillas. Sin modelo ni migración. Router en main.py, docs/api.md actualizado. 10 tests nuevos (7 servicio + 3 endpoint), 521 en verde. Reviewer: APPROVED.

- **Feature 35 · auth_refresh_tokens** (2026-08-08) — Access token corto (JWT 15 min) + refresh opaco persistido y rotatorio con revocación y detección de reuse. Modelo RefreshToken (solo hash sha256), migración 0015, POST /auth/login (par), /auth/refresh (rotación), /auth/logout (revoca idempotente). Reuse → 401 + revocación de cadena. Reviewer: APPROVED. 532 tests verdes.2026-08-08 | feat_36 account_recovery | Verificación de email y reset de password con tokens de un solo uso (tabla account_tokens: hash sha256, purpose verify/reset, expires_at, consumed_at — nunca en claro) + migración 0016 (down_revision 0015, users.email_verified bool default false). Interfaz EmailSender aislada (backlogg/core/email.py): transporte SMTP genérico de la stdlib (SmtpEmailSender: smtplib + EmailMessage, starttls/login condicionales, send_message en asyncio.to_thread) cuando SMTP_HOST está configurado, LoggingEmailSender fallback que loguea el enlace si no. SIN dependencias nuevas (se descartó Resend a mitad: dep resend eliminada). Endpoints POST /auth/verify/request (auth), /auth/verify/confirm, /auth/password/forgot (202 siempre, sin enumeración), /auth/password/reset (cambia hash + revoca refresh tokens de feat_35). Single-use + expiración → reuso 400; fallo SMTP no filtra ni rompe (degradación elegante, token commiteado antes de enviar); SMTP_PASSWORD nunca en logs. Settings SMTP_HOST/PORT/USERNAME/PASSWORD/FROM_EMAIL/STARTTLS + APP_BASE_URL + EMAIL_VERIFY_EXPIRE_HOURS/PASSWORD_RESET_EXPIRE_HOURS. Docs (schema/api/operations/external-apis, nota Gmail App Password para pruebas y dominio propio a futuro) + .env.example + 5 requests bruno + 24 tests. 556 tests en verde. Reviewer: APPROVED. QA manual con SMTP real (servidor de captura local): verify/reset end-to-end, no-enumeración confirmada en el transporte (0 correos al email inexistente).

2026-08-08 | feat_37 rate_limiting | Nueva capa core backlogg/core/rate_limit.py (RateLimiter ABC + InMemoryRateLimiter sliding-window-log con reloj inyectable y reset(); RateLimitResult; parse_rate "10/60"; get_rate_limiter() singleton — swap a Redis toca solo la factory; client_ip con X-Forwarded-For; dependency rate_limit_auth + enforce_search_fallback; detail 429 estático sin filtrar IP/límites, solo Retry-After variable). Wiring: dependencies=[Depends(rate_limit_auth)] SOLO en POST /auth/login y /register (refresh/logout/verify/reset intactos); gating del fan-out externo de /search dentro de service.py justo antes del fan-out y SOLO cuando total==0 (queries con resultados locales no consumen cupo). Settings RATE_LIMIT_AUTH/DEFAULT/SEARCH_FALLBACK (defaults generosos). conftest: fixture autouse reset del limiter por test (los tests comparten IP testclient). Docs api/operations + .env.example. 26 tests nuevos (unit limiter+parse_rate, endpoint auth 429+reset tras ventana con reloj falso, search fallback), 581 en verde. Reviewer: APPROVED (8 acceptance + checkpoints verificados contra código).

2026-08-08 | feat_39 metrics_endpoint | GET /metrics en formato de exposición Prometheus v0.0.4, stdlib-only (sin prometheus-client ni deps nuevas, coherente con feat_38/feat_36). backlogg/core/metrics.py (MetricsRegistry in-process thread-safe con un threading.Lock, singleton get_metrics() reemplazable estilo get_rate_limiter, serializador manual del texto de exposición, MetricsMiddleware) + backlogg/metrics/routes.py (router fino, PlainTextResponse content-type text/plain; version=0.0.4). Familias: http_requests_total{method,path,status}, http_request_duration_seconds{method,path} (histograma con buckets acumulativos _bucket{le}/_sum/_count), backlogg_syncs_total{type} (incrementado en los 4 sync jobs de scheduler/jobs.py), backlogg_external_fanout_total{source} (incrementado en los 4 _ingest_* del fan-out externo de search/service.py). Label path = PLANTILLA de ruta (request.scope["route"].path → /movies/{slug}), nunca la URL real; 404 → __unmatched__; sin query strings/headers/PII en labels. /metrics excluido de su propia instrumentación (evita ruido auto-referencial); grabación en finally para contar también los 500. MetricsMiddleware añadido antes de RequestIDMiddleware (RequestID sigue siendo el más externo; CORS/SecurityHeaders intactos). Sin settings nuevos (endpoint siempre activo). docs/api.md + docs/operations.md + bruno/Metrics/. 15 tests nuevos (registro/buckets/escapado/HELP-TYPE, endpoint 200+content-type+parseable, plantilla-no-URL, __unmatched__, exclusión propia, no-regresión, incremento real de contadores de negocio, no-PII), 614 en verde. Reviewer: APPROVED (22 checkpoints aplicables + 8 puntos de atención verificados contra código).

2026-08-08 | feat_41 openapi_polish | Enriquecido el esquema OpenAPI generado (solo metadatos, sin cambios de contrato: paths/métodos/status/response_model/campos intactos). backlogg/main.py: FastAPI ahora declara version="0.1.0" (alineado con pyproject.toml), description multilínea (constante API_DESCRIPTION: catálogo unificado + capa social, nota auth Bearer y admin X-API-Key) y openapi_tags=OPENAPI_TAGS (constante de módulo, 19 dominios con {name,description}; orden de include_router sin tocar). summary= conciso en cada decorador de ruta de los 18 routers + description= donde aporta (fallback on-demand, auth req/opcional, idempotencia, rate limiting, no-enumeración, owner-only). Ejemplos json_schema_extra (ConfigDict Pydantic v2, combinado con from_attributes donde existía) en schemas de respuesta clave: MovieOut, MovieListOut, SearchResponse, GenreListOut, TrendingOut, FeedListOut, LibraryStatusOut, LibraryListOut, UserListOut, UserListsOut, NotificationListOut, RecommendationsOut. tests/test_openapi_schema.py (6 tests): OpenAPI 3.x, info title/version/description, 19 tags con descripción, summaries en operaciones clave (GET /movies/{slug}, POST /auth/login, GET /feed, GET /search, GET /recommendations, POST /lists), presencia de example en components.schemas. 638 tests en verde. Reviewer: APPROVED (6 acceptance + checkpoints verificados; nota no bloqueante: el test de examples exige "al menos uno", podría endurecerse).

2026-08-08 | feat_42 account_deletion | DELETE /users/me (204, auth requerida) — borrado de cuenta GDPR. backlogg/users/routes.py (endpoint registrado ANTES de GET /users/{username}) + users/service.py delete_current_user (captura items rateados → borra fila usuario con flush → recomputa agregados → commit atómico) + users/repository.py delete_user + ratings/repository.py get_distinct_rated_items_for_user. SIN migración: los 8 FKs a users.id ya son ON DELETE CASCADE (user_ratings, review_likes, follows×2, library_entries, user_lists, notifications×2, refresh_tokens, account_tokens). El recompute corre SQL fresco tras el flush, no ve las filas borradas. docs/api.md + 2 requests bruno + 7 tests (401 sin/invalid token, 204+perfil 404, re-registro con mismo username/email, refresh 401 tras borrado, cascada verificada sobre las 8 tablas + datos de otro usuario sobreviven, recompute 3.0/2→2.0/1). 645 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev, server vivo): 11/11 checks incl. recompute count→0/rating→None, refresh 401, access token inválido tras borrado, username/email liberados.

2026-08-08 | feat_43 review_reports | Nuevo dominio backlogg/reports/ (models ReviewReport, schemas ReportStatus StrEnum/ReportReasonIn/ReportOut/ReportListOut, repository, service, routes con reports_router de usuario + admin_reports_router propio prefix /admin con Depends(verify_api_key)) + migración 0017_review_reports (down_revision 0016; tabla review_reports: reporter_id FK→users.id CASCADE, rating_id FK→user_ratings.id CASCADE, reason VARCHAR(300) nullable, status VARCHAR(20) CHECK(open/resolved) default open, created_at/resolved_at, UniqueConstraint(reporter_id,rating_id)=uq_review_report_pair, idx_review_reports_status). Endpoints: POST /reviews/{id}/report (auth; 201 primera vez / 200 idempotente mismo (reporter,review) preservando reason; body opcional; 404 review inexistente vía ratings.get_rating_by_id; 401 sin token), GET /admin/reports?status=&page=&limit= (X-API-Key, filtro estado + paginación, recientes primero), POST /admin/reports/{id}/resolve (X-API-Key, sella resolved+resolved_at, 404 inexistente). main.py registra ambos routers + tag OpenAPI reports. docs/schema.md + docs/api.md + 4 requests bruno + 24 tests (repo 6, service 6, routes 12). 669 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev migrada a 0017, server vivo): 13/13 incl. idempotencia (mismo id/reason preservado), body vacío, filtro/resolve admin, 401 sin key, y cascada rating_id (borrar la review elimina su reporte).

2026-08-08 | feat_44 content_moderation | Migración 0018 (down_revision 0017): user_ratings.is_hidden y users.is_banned (ambos NOT NULL DEFAULT false). Nuevo slice backlogg/moderation/ (schemas ReviewModerationOut/UserModerationOut, service set_review_hidden/set_user_banned, admin_moderation_router prefix /admin con verify_api_key — sin tabla propia, orquesta ratings.repository + users.repository). Condición de visibilidad reutilizable ratings.repository.visible_review_filters() (review visible sólo si is_hidden=false Y autor is_banned=false; requiere JOIN a users) aplicada de forma coherente en recalculate_item_aggregates (AVG/COUNT), list_ratings_for_item (count+paginada), list_user_reviews (cada rama del UNION ALL) y feed (list_following_feed + list_popular_feed). Recompute: hide/unhide recomputa el item de la review; ban/unban captura los items del usuario con get_distinct_rated_items_for_user (reusado de feat 42) y recomputa cada uno, commit atómico. Enforcement auth: usuario baneado → 401 genérico en login_user y en refresh_tokens (no revela el baneo, coherente con no-enumeración; en refresh el token no se rota). Endpoints admin (X-API-Key, idempotentes, 404 si no existe): POST /admin/reviews/{id}/hide|unhide (200 {id,is_hidden}), POST /admin/users/{username}/ban|unban (200 {username,is_banned}). main.py registra el router. docs/schema.md + docs/api.md + 5 requests bruno + 19 tests (17 endpoint + 2 repo). 688 tests en verde. Reviewer: APPROVED. QA manual (leader, DB dev migrada a 0018, server vivo): 18/18 — hide/ban excluyen de ratings+user reviews y recomputan agregado (cnt→0), login/refresh baneado 401, y unhide/unban restauran review y agregado.

2026-08-08 | feat_45 api_versioning | Barrido final: TODA la superficie de negocio bajo /v1. main.py aplica prefix="/v1" en los 23 include_router de negocio (movies, series, books, games, people, search, admin, genres, trending, auth, users, ratings, user_reviews, follows, feed, user_library, lists, user_lists, notifications, recommendations, reports, admin_reports, admin_moderation); cada router ya traía su prefix relativo → /v1/movies, /v1/auth/login, /v1/admin/reports, etc. Corte limpio SIN alias en la raíz. Exceptuados (ops, sin versionar): @app.get("/health") y metrics_router (/metrics). Ajuste interno obligado: backlogg/core/http_cache.py clasifica respuestas por route template (request.scope["route"].path), que ahora incluye el prefijo → DETAIL/LISTING/PRIVATE_TEMPLATES y _listing_max_age() actualizados a /v1/... (si no, la política de Cache-Control/ETag habría dejado de aplicar en silencio). NO se tocó users/service.py::_link (enlaces de frontend verify-email/reset-password sobre APP_BASE_URL, no endpoints de API). docs/api.md (79 paths + nota de política), docs/architecture.md (5), docs/operations.md (12) alineados; bruno 141 .bru con {{baseUrl}}/v1/..., Health/Metrics intactos. Tests actualizados a /v1 (incluidas plantillas en tests de metrics y paths en el test de OpenAPI de feat 41). 688 tests en verde. Reviewer: APPROVED. QA manual (leader, server vivo): 18/18 — /health y /metrics sin versionar, negocio bajo /v1, rutas raíz viejas → 404, openapi.json con paths /v1 y /health sin versionar. Con esto se cierra el backend inicial (features 1–45); siguiente etapa: frontend Next.js + React + TS consumiendo /v1.
