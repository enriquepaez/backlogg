# Sesión actual

Sin feature en curso. **FE-11 `global_search` cerrada** (reviewer APPROVED, `done` en
`frontend_feature_list.json`), rama `feat/fe11-global-search`, pendiente:
1. QA manual en navegador (checklist pendiente de confirmación del usuario).
2. Confirmación de ship (commit + push + PR a main).

## Siguiente feature disponible (M1)
**FE-12** `genres_trending_browse` (id=13, depende de FE-6 `app_shell` ✅ y FE-2 `api_client` ✅ —
ambas satisfechas).
