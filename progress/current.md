# Sesión actual

Sin feature en curso. **FE-13 `register_login` cerrada** (reviewer APPROVED,
`done` en `frontend_feature_list.json`), rama `feat/fe13-register-login`.
QA manual en navegador completada por el usuario (10 rondas de ajustes UX
sobre la presentación de errores, todas verificadas empíricamente contra el
backend y frontend reales — ver `progress/history.md`, entrada FE-13, para
el detalle completo). Ship en curso: commit + push + PR a main.

## Siguiente feature disponible (M2)
**FE-14** `session_ux` (id=15, depende de FE-13 ✅).
