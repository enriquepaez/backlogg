# Sesión actual

Sin feature en curso.

Feature 33 `notifications` completada, aprobada y shipped (PR #59, rama
`feat/notifications`, pendiente de merge a main). Incluye fix del harness de
tests (aislamiento por SAVEPOINT en `tests/conftest.py`).

Siguiente feature elegible por dependencias: **34 personalized_recommendations**
(depende de 31, 28, 16 — todas `done`). También elegibles: 35 auth_refresh_tokens,
36 account_recovery (dependen de 27, `done`).
