# Sesión actual

Sin feature en curso. **FE-7 `testing_ci` cerrada**: PR #83 mergeado a `main` (checks `test` y
`frontend` en verde en GitHub Actions), rama `feat/fe7-testing-ci` borrada (local + remota).
Backend (`feature_list.json`): 45/45 `done`. Frontend M0 (FE-1..FE-7) completo.

## Siguiente feature disponible (M1)
**FE-8** `home_landing` (id=9, depende de FE-2 `api_client` ✅ y FE-5 `app_shell` ✅ — ambas
satisfechas). Arranca M1 (catálogo público / SEO): home con trending y destacados por tipo, SSR/ISR.
