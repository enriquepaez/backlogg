# Sesión actual

Sin feature en curso. **FE-10 `item_detail` + 2 bugfixes bundled** (`book_credits`,
`book_credits_empty_crash`) shipped en un solo commit en `feat/fe10-item-detail`. PR abierto a
`main`: https://github.com/enriquepaez/backlogg/pull/86.

## Siguiente feature disponible (M1)
**FE-11** `global_search` (id=12, depende de FE-6 `app_shell` ✅ y FE-2 `api_client` ✅ — ambas
satisfechas).
