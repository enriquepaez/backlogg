# Sesión actual

Sin feature en curso. **FE-8 `home_landing` cerrada**: reviewer APPROVED, marcada `done` en
`frontend_feature_list.json`, resumen movido a `progress/history.md`. Pendiente: QA manual del
usuario en navegador y confirmación de ship (commit + push + PR) en la rama `feat/fe8-home-landing`.

## Siguiente feature disponible (M1)
**FE-9** `browse_by_type` (id=10, depende de FE-6 `app_shell` ✅ y FE-2 `api_client` ✅ — ambas
satisfechas). Browse por tipo ×4 (filtro por género, sort, paginación) — SSR/ISR. Los enlaces
"ver todo" añadidos en FE-8 (`/browse/{type}`) empiezan a resolver con esta feature.
